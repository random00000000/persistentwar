# Decision Making Document

This file stores decisions that should later be reviewed by the human and translated into new directives.

Use it when the project benefits from delayed human decisions instead of immediate synchronous answers.

## Current Decisions
- `Open`: Should the new `Anchor-break package` / `Break the plated anchor first` coaching stay explicit whenever Broken Signal turns into a live AMR counter-lane, or should that plate-break grammar quiet down once players internalize the repeek pattern?
  - `Reason`: This run promoted the AMR from a stash-only doctrine proof into a real raid-side plate-break slice. The new `amr-counter-lane` showcase stages Broken Signal as a focused `Relay counter lane` problem with a plated repeek owner locking Dish Houses, `Pressure Posture` explicitly says `Break the plated anchor first`, `Operation Flow` carries the same commitment, and the boys flip into an `Anchor-break package` so the AMR is solving a live lane instead of only reading well on the wall. The unresolved product choice is whether that explicit plate-break coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same answer from the plated threat, crossing window, and AMR loadout alone. The latest evidence is `automation-artifacts/amr-counter-lane-2026-04-20.png` and `automation-artifacts/amr-counter-lane-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `Room flood package` / `Dish house breach` coaching stay explicit whenever Broken Signal turns into a tight room-fight, or should that CQB grammar quiet down once players internalize the inside-flood versus yard-lid split?
  - `Reason`: This run added the missing raid-side CQB proof for the gun-doctrine package instead of relying on stash-side PKM and sidearm boards plus generic room-clear telemetry. The new `dish-house-breach` showcase turns `Dish Houses` into a real SMG-led room flood with a shotgun corner break, the operation read explicitly says `Flood the first room`, and `combat.squadDoctrine` now renames the mixed stack into `Room flood package` with `Room flood`, `Door breaker`, and `Yard lid` roles. The unresolved product choice is whether that explicit CQB coaching should remain a stable part of the game's identity, or whether veteran players should eventually infer the same doctrine from the subzone, weapon mix, and active threat picture alone. The latest evidence is `automation-artifacts/dish-house-breach-2026-04-20.png` and `automation-artifacts/dish-house-breach-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `Mortar break package` / `Break the tube before next bracket` coaching stay explicit whenever Ambulance Mile turns into a shell-walk lane, or should that mortar-language quiet down once players internalize the bracket pattern?
  - `Reason`: This run added a first-class mortar-pressure proof instead of leaving artillery escalation as future-only direction text. The new `mortar-bracket` showcase turns `Clinic bracket crew` into the live strip problem, `Pressure Posture` explicitly says `Break the tube before next bracket`, `Operation Flow` treats the shell walk as the commitment problem instead of generic trench pressure, and the boys flip into a dedicated `Mortar break package` with `Tube lid`, `Glass break`, and `Underpass runner` roles so the clinic cut, med-lane lip, and underpass peel all read like one authored shell break. The unresolved product choice is whether that explicit mortar coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same bracket danger from crater walks, tube clutter, and underpass peel timing alone. The latest evidence is `automation-artifacts/mortar-bracket-2026-04-20.png` and `automation-artifacts/mortar-bracket-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `Tape-cut package` / `Kill zone strip package` coaching stay explicit whenever the dock lane turns into a shouted tape argument or the road becomes a burning convoy salvage strip, or should that lane-specific grammar quiet down once players internalize the patterns?
  - `Reason`: This run promoted two existing screenshot-only war beats into first-class verified north-star proof instead of leaving them as atmosphere. `hostile-lane-chatter` now stamps a focused `hostile-lane-chatter` incident, `Pressure Posture` explicitly says `Cut the loudest tape first`, `Operation Flow` treats the dock argument as the active commitment problem, and `verify --id hostile-lane-chatter` proves the lane is reading as a real tape-led tactical problem. `caravan-trap` now gets matching shared-grammar treatment with `Strip the kill zone fast`, a dedicated `Kill zone strip package`, and `verify --id caravan-trap` proving the road is a live salvage-under-fire commitment instead of background wreckage. The unresolved product choice is whether that explicit dock-argument and convoy-salvage language should remain part of the route identity long term, or whether veteran players should eventually infer the same truth from the shouting traffic, burning wagons, and reserve timing alone.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new debrief-side `Disciplined clean peel` versus `Collapse-taxed extract` labeling stay explicit after players learn the extraction arc, or should the stash eventually quiet back down to shorter operation summaries?
  - `Reason`: This run stopped inferring extraction discipline from rough end-state heuristics and instead recorded the live shared operation grammar directly into raid summaries. Successful pulls can now come home as a real `Disciplined clean peel` or `Collapse-taxed extract`, and `verify --id extract-clean` plus `verify --id extract-collapse` prove the paired showcase states before they ever hit stash. The unresolved product choice is whether that explicit clean-vs-collapse language should remain part of the game's operational identity long term, or whether veteran players should eventually infer the same truth from route cleanliness, casualty cost, and stash swing alone. The latest evidence is `automation-artifacts/extract-clean-verify-2026-04-20.png` and `automation-artifacts/extract-collapse-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new hunter-search coaching stay explicit whenever the squad is guiding the old man off the reeds, or should that rescue-language quiet down once players internalize the pattern?
  - `Reason`: This run promoted `hunter-search` from a screenshot-only war story into a first-class verified proof. `Pressure Posture` now explicitly tells the player to `Guide the hunter off the reeds`, `Operation Flow` treats the treeline rescue as the live commitment problem instead of generic escort copy, and the boys flip into a dedicated `Hunter screen package` so the shack mouth, reed screen, and route-note pull all read like one protected human beat. The unresolved product choice is whether that explicit rescue coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same lane from the hunter, lantern, and squad screen alone. The latest evidence is `automation-artifacts/hunter-search-2026-04-20.png` and `automation-artifacts/hunter-search-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new chair-handoff board stay explicit about which seat can turn now versus which seat is still blocked by grief follow-through, or should that replacement-language quiet down once players internalize the ritual grammar?
  - `Reason`: This run promoted the stash-normalization package's chair-handoff track into a first-class proof slice instead of leaving replacement pressure split across generic operator and debrief states. The new `chair-handoff` showcase stages one settled seat that can turn to Lev immediately, one missing-boy chair still blocked by Makar's unanswered family call, and `verify --id chair-handoff` proves the stash is surfacing `replacementPending`, a live turnable chair, and unresolved memorial debt in the same operational board. The unresolved product choice is whether that explicit handoff grammar should remain part of the stash identity long term, or whether veteran players should eventually infer the same state from the memorial wall and reserve queue alone. The latest evidence is `automation-artifacts/chair-handoff-2026-04-20.png`, `automation-artifacts/chair-handoff-board-2026-04-20.png`, and `automation-artifacts/chair-handoff-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `shed-hide` coaching stay explicit whenever a live village sweep is forcing the squad to freeze inside the tin shed, or should that hide-window language quiet down once players internalize the infiltration pattern?
  - `Reason`: This run promoted the existing `shed-hide` authored beat from a screenshot-only infiltration story into a first-class north-star proof. `Operation Flow` now treats `Tin shed hide` as the live commitment problem, `Pressure Posture` explicitly says `Stay dark in the shed`, the boys flip into a dedicated `Hide pocket package`, and `verify --id shed-hide` proves the fragile Relay Hamlet sweep, hide window, and squad doctrine are all reading the same quiet-under-pressure lane. The unresolved product choice is whether that explicit hide-pocket coaching should remain a standing readability layer for these infiltration beats, or whether veteran players should eventually infer the same state from the shed slit, Blue sweep chatter, and quieter squad posture alone. The latest evidence is `automation-artifacts/shed-hide-2026-04-20.png` and `automation-artifacts/shed-hide-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `deadzone-breaths` quiet-life chatter stay explicit in coffee pockets, shelter lanes, and soft extract windows, or should that breathing-room language quiet down once players internalize the contrast grammar?
  - `Reason`: This run advanced the rimworld-dialogue package's `Quiet Life And Contrast` milestone by adding a fourth reusable story pack, `deadzone-breaths`, instead of widening resolver logic. The pack stays inside existing `advance`, `contact`, `coffee`, `civilian`, and extract event kinds, but it gives `field-coffee`, `burner-coffee`, `civilian-window`, and quiet approach/search states a stronger shared voice so the campaign can breathe between firefights without muting tactical stakes. A follow-up verification pass now promotes the coffee beats into first-class proof instead of leaving them as screenshot-only flavor: `field-coffee` and `burner-coffee` both stage stable quiet-life comms, coffee-pocket reads, and authored memory tags through CLI verification. The unresolved product choice is whether that explicit breathing-room dialogue should remain part of the route identity long term, or whether veteran players should eventually infer the same calm-to-violence contrast from pacing and a lighter comms layer alone. The latest evidence is `automation-artifacts/field-coffee-verify-2026-04-20.png`, `automation-artifacts/burner-coffee-verify-2026-04-20.png`, and `automation-artifacts/deadzone-breaths-burner-coffee-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new relay-cellar `Cellar hold package` / `Hold the cellar mouth` coaching stay explicit whenever one bunker-chain foothold is the only stable anchor in Relay Hamlet, or should that bunker-mouth language quiet down once players internalize the cellar-counterhold pattern?
  - `Reason`: This run promoted the relay bunker-chain from a calmer reset beat into a real replayable foothold problem instead of leaving bunker grammar at `bunker-foothold` alone. The new `cellar-counterhold` showcase stages `Relay Cellar` as the only stable foothold in `Relay Hamlet`, `Pressure Posture` now explicitly tells the player to `Hold the cellar mouth`, `Operation Flow` treats the cellar as the committed foothold while reserve rifles try to shove back through the dish lane, and the boys flip into a dedicated `Cellar hold package` instead of generic retake copy. The unresolved product choice is whether that explicit bunker-mouth coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same foothold state from the cellar, route note, and reserve shove alone. The latest evidence is `automation-artifacts/cellar-counterhold-2026-04-20.png` and `automation-artifacts/cellar-counterhold-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should memorial follow-through coaching stay explicit in live raid chatter after players use `Call Family` and `Hold Wake`, or should that aftermath language quiet down once players learn the ritual grammar?
  - `Reason`: This run connected the stash memorial wall to the next raid instead of letting those rites die in the operator tab. `Call Family` now stamps `family-informed`, `Hold Wake` now stamps `wake-held`, the reusable `after-action-voices` pack can spend those tags in live contact/extract chatter, and `verify --id dialogue-aftermath` proves the next hot pull sounds like a squad carrying real aftermath into the lane rather than resetting to generic exfil stress. The unresolved product choice is whether that explicit memorial coaching should remain a standing readability layer whenever the squad is still emotionally carrying those rites, or whether veteran players should eventually infer the same state from subtler comms alone. The latest evidence is `automation-artifacts/dialogue-aftermath-2026-04-20.png` and `automation-artifacts/dialogue-aftermath-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new cellar-family escort coaching stay explicit whenever a live civilian evacuation is holding together around the cellar curb and white van peel, or should that escort-language quiet down once players internalize the pattern?
  - `Reason`: This run promoted `civilian-window` from a screenshot-only war story into a first-class shared-grammar proof. `getRaidOperationRead` now treats the family move as a live escort contract instead of profit greed, `Pressure Posture` explicitly tells the player to `Walk the family to the van`, the boys flip into a dedicated `Civilian escort package`, and `verify --id civilian-window` proves the cellar mouth, escort lane, and waiting van are all reading the same under-fire evacuation problem. The unresolved product choice is whether that explicit escort coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same state from the shelter block and van curb alone. The latest evidence is `automation-artifacts/civilian-window-2026-04-20.png` and `automation-artifacts/civilian-window-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new final-assault `Entry Belt` / `Relay Mouth` / `Casualty Strip` / `Escape Cut` coaching stay explicit once the player reaches the stronghold, or should the finale eventually quiet down after the wall grammar is learned?
  - `Reason`: This run closed the main readability gap between finale readiness and finale launch instead of leaving `final-stronghold-launch` as only a stronger status line. `campaign.finale` now exposes a shared `assaultPlan`, the stash operations board and briefing surface those launch beats directly, and `verify --id final-stronghold-launch` proves the breach is reading like a real committed wall with live entry, corridor, and peel asks. The unresolved product choice is whether that explicit four-beat assault coaching should remain part of the endgame identity long term, or whether veteran players should eventually read the same wall more implicitly. The latest evidence is `automation-artifacts/final-stronghold-launch-assault-plan-2026-04-20.png` and `automation-artifacts/final-stronghold-launch-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new Ambulance Mile trench-reserve coaching stay explicit whenever a fragile trench-line return is leaning on one med-lane lip plus one underpass foothold, or should that strip-specific counterhold language quiet down once players internalize the pattern?
  - `Reason`: This run broadened the replayable-settlement grammar into the project's trench-line route instead of keeping return-state proof mostly on town blocks and compounds. The new `ambulance-counterhold` showcase stages `Ambulance Mile` as a fragile return where `Med Lane Trench` still has to hold before reserve bodies spill out of the shell yard, `Underpass strongpoint` is the only settled reset pocket behind the strip, `Pressure Posture` now calls for `Brace the trench reserve lane`, and `verify --id ambulance-counterhold` proves that settlement volatility, trench-reserve pressure, operation flow, and `Trench shove package` doctrine all read the same counterhold problem. The unresolved product choice is whether that explicit trench-reserve coaching should remain a standing readability layer for fragile strip returns, or whether veteran players should eventually infer the same state from the trench and underpass alone. The latest evidence is `automation-artifacts/ambulance-counterhold-2026-04-20.png` and `automation-artifacts/ambulance-counterhold-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new clinic-side mortar bracket coaching stay explicit whenever a live tube team is walking shells onto Ambulance Mile, or should that break-the-tube language quiet down once players internalize the shell-walk pattern?
  - `Reason`: This run finished the north-star shell-pressure proof by turning the half-landed `mortar-bracket` slice into a clean verified beat instead of leaving it as a staged showcase plus a brittle verifier. The runtime already staged `Clinic bracket crew` as the live firefight, `Pressure Posture` explicitly said `Break the tube before next bracket`, `Operation Flow` treated the shell walk as the active strip problem, and the boys flipped into a dedicated `Mortar break package`; the missing issue was that the snapshot surface dropped frontline `presentationVariant`, which made `verify --id mortar-bracket` fail even though the slice was live. Snapshot now carries that variant through the frontline incident read, `verify --id mortar-bracket` passes, and the resulting proof cleanly shows the clinic tube, med-lane lip, and underpass peel reading as one shell-break decision. The unresolved product choice is whether that explicit tube-break coaching should remain a standing readability layer for shell-walk beats, or whether veteran players should eventually infer the same crisis from the bracket impacts, clinic cut, and reserve timing alone. The latest evidence is `automation-artifacts/mortar-bracket-2026-04-20.png` and `automation-artifacts/mortar-bracket-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the finale setback state keep explicit spent-prep coaching like `Setback tax` and damaged retry-track language, or should that retry readability quiet down once players internalize what a failed launch burns?
  - `Reason`: This run turned `final-stronghold-setback` into a real failure-and-retry state instead of a paragraph swap. `campaign.finale` can now mark the safer lane and casualty corridor as spent, damage `Preparation orders` and `Squad depth`, apply a readiness penalty, and `verify --id final-stronghold-setback` proves the campaign comes back degraded but still recoverable. The remaining product choice is whether that explicit retry-coaching grammar should remain part of the endgame identity long term, or whether veteran players should eventually infer the same loss from the remaining prep stack alone. The latest evidence is `automation-artifacts/final-stronghold-setback-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the stronger tracer, blast, grenade, and material-break readability in the new combat-presentation pass stay this explicit in live raids, or should some of that danger language quiet down once players internalize the lane?
  - `Reason`: This run closed the remaining combat-center VFX proof gap instead of leaving `combat-presentation` as a screenshot-only scene pass. Snapshot now exposes `combat.presentationRead`, the CLI can `verify --id combat-presentation` against mixed concrete-and-dust impact reads plus live suppression, blast shock, and hostile frag danger, and the Phaser raid view now makes those states more legible with stronger suppression chevrons, blast shock rings, grenade threat spokes, and faction-colored tracers. The remaining product choice is whether that explicit effect grammar should stay part of the raid's readability identity, or whether veteran players should eventually read the same lane more implicitly. The latest evidence is `automation-artifacts/combat-presentation-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new Relay Hamlet counterpush coaching stay explicit whenever a reclaiming compound still has a routed pocket plus a live reserve lane, or should that reserve-aware retake language quiet down once players internalize the pattern?
  - `Reason`: This run broadened the replayable-settlement proof beyond Customs Quay instead of letting the same district carry every return-state beat. The new `relay-counterpush` showcase stages Relay Hamlet as a reclaiming compound where one routed dish-house pocket still needs to be locked before the antenna-ditch reserve lane pushes back through the route, `Pressure Posture` stays on a surrender window with a live `Reserve in ...` threat read, and `verify --id relay-counterpush` proves the boys still read the route as a `Retake hold package` instead of flattening into generic trench or peel logic. The unresolved product choice is whether that explicit reserve-aware retake coaching should remain part of the route identity long term, or whether veteran players should eventually infer the same pattern from the compound state alone. The latest evidence is `automation-artifacts/relay-counterpush-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new surrender-window coaching stay explicit whenever a routed pocket can be secured for route intel, or should that reclaim language quiet down once players internalize when a broken lane is safer to secure than to keep farming?
  - `Reason`: This run closed the obvious verification gap in the AI-pressure package's surrender slice instead of leaving `surrender-window` as only a screenshotable authored beat. The CLI can now `verify --id surrender-window` against the live `surrendering` pressure posture, the routed `secure` pocket with route-intel payoff, the `surrender-pocket` settlement memory, and the convoy-plus-casualty pressure still living behind the lane. The remaining product question is whether that explicit secure-the-surrender coaching should remain a standing readability layer whenever a broken pocket is ready to be claimed, or whether veteran players should eventually read that opportunity more implicitly. The latest evidence is `automation-artifacts/surrender-window-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the stash keep explicit realized-credit coaching like `+160 cr realized` and sold-state `Broker Weight` language after players internalize the cash-out loop, or should that broker confirmation quiet down once the wall feels operational by default?
  - `Reason`: This run completed the missing stash-side cash-out half of the recovered-haul loop instead of leaving hot cargo and captured guns as tagged-but-passive shelf props. Broker-tagged sale items and captured field weapons can now be sold directly off the rack for real credits, the stash chip and `Operational Wall` board surface realized broker money in the same cycle, and `verify --id broker-cashout` now proves that a recovered PKM can become deploy money instead of only a hold-or-sell thought exercise. The unresolved product choice is whether that explicit realized-credit feedback should remain part of the stash identity long term, or whether veteran players should eventually see a quieter wall once they understand the cash-out grammar. The latest evidence is `automation-artifacts/broker-cashout-2026-04-20.png` and `automation-artifacts/broker-cashout-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new armored-evac wording stay explicit in `Operation Flow` and `Pressure Posture` whenever a Blue wagon is trying to swallow the casualty strip, or should that wagon-specific coaching quiet down once players internalize the medevac pattern?
  - `Reason`: This run promoted the authored `armored-evac` beat from a screenshot-only war story into a verified shared-grammar slice. `getRaidOperationRead` and `getPressurePostureRead` now explicitly call out the evac wagon whenever a live casualty lane and extracting convoy are occupying the same strip, and `verify --id armored-evac` proves that operation flow, posture, and doctrine all read the same medevac-versus-recovery problem. The remaining product choice is whether that wagon-specific wording should remain part of the raid's readability language long term, or whether veteran players should eventually infer the same pattern from the strip itself. The latest evidence is `automation-artifacts/armored-evac-2026-04-20.png` and `automation-artifacts/armored-evac-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new stash-side `Field Weapon` board keep explicit hold-versus-broker coaching whenever a recovered enemy gun comes home, or should that recommendation quiet down once players internalize how route fit and stash pressure shape the choice?
  - `Reason`: This run deepened the gun-doctrine package's looted-weapon pass instead of leaving captured guns as one rack tile plus a sentence. The stash loadout pane now compares the recovered gun against the staged primary, calls out the best pivot route, and recommends whether to pivot now, hold the gun as doctrine, or sell it for wall money, with the same read exposed in `stash.recoveredHaulSummary.fieldWeaponDecision`. The unresolved product choice is whether that explicit coaching should remain a standing readability aid for captured-gun decisions, or whether veteran players should eventually read the rack more implicitly. The latest evidence is `automation-artifacts/field-capture-decision-2026-04-20.png` and `automation-artifacts/field-capture-verify-2026-04-20b.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new live squad-package names like `Recovery corridor package`, `Trench shove package`, `Retake hold package`, and `Settlement peel package` stay explicit in the doctrine board and per-boy cards, or should that language quiet back down once players internalize the contextual reads?
  - `Reason`: This run moved the boys package out of generic route-fit copy and into the same shared state language as operation flow, settlement volatility, and trench pressure. The same loadout can now rename itself based on the live raid truth, which makes recovery pulls, trench shoves, retakes, and hot peels much easier to read in the moment. The unresolved product choice is whether that explicit naming should remain part of the game's identity long term, or whether veteran players should eventually see a quieter doctrine surface once the contextual grammar is learned. The latest evidence is `automation-artifacts/body-recovery-doctrine-verify-2026-04-20.png`, `automation-artifacts/trench-assault-doctrine-verify-2026-04-20c.png`, `automation-artifacts/territory-retake-doctrine-verify-2026-04-20.png`, and `automation-artifacts/retake-peel-doctrine-verify-2026-04-20b.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should bunker footholds keep the explicit `hold-position` / peel-relief coaching whenever the squad settles a concrete reset pocket, or should that bunker-exfil guidance quiet down once players learn that these underpass holds buy cleaner exits?
  - `Reason`: This run strengthened the north-star bunker foothold slice so it stops reading like atmosphere-only shelter. The authored `bunker-foothold` showcase now stages a live `settle` action, the boys actively holding the concrete mouth, a taped-mag handoff inside the bunker, and measurable exfil relief under lower noise pressure, and `verify --id bunker-foothold` now proves that calmer under-fire reset state directly from snapshot data. The unresolved product choice is whether that explicit bunker-hold coaching should remain a standing readability layer whenever a concrete pocket is buying a cleaner peel, or whether veteran players should eventually just feel the payoff without the extra wording. The latest evidence is `automation-artifacts/bunker-foothold-2026-04-20.png` and `automation-artifacts/bunker-foothold-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should body-driven raids keep explicit `recovery peel` / casualty-corridor coaching in the live extract recommendations and `Operation Flow`, or should that wording quiet down once players internalize that body claims and wounded drags bend the whole exit plan?
  - `Reason`: This run fixed a coherence break in the recovery package. The authored `body-recovery` slice was already staging a live casualty pull and a `recovering` pressure posture, but `raid.operationExitIntent` could still read like a normal profit exit. `getRaidOperationRead`, extract recommendations, and planned extract commit staging now all classify those situations as recovery-driven, and `verify --id body-recovery` proves the raid reads as a recovery-first peel from start to debrief. The unresolved product choice is whether that explicit recovery phrasing should remain a standing readability layer whenever a corpse or casualty is bending the route, or whether veteran players should eventually see a quieter extract surface. The latest evidence is `automation-artifacts/body-recovery-operation-flow-2026-04-20.png` and `automation-artifacts/body-recovery-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should settlement-aware peel coaching stay explicit in the live `Operation Flow` and extract-choice summaries whenever a town is fragile, reclaiming, or lost, or should that extra settlement-specific extract warning collapse once players internalize that unstable ground heats exfil faster?
  - `Reason`: This run connected the AI-pressure and extraction packages directly instead of leaving the settlement return read and the exfil grammar as parallel systems. `getRaidOperationRead` now taxes operation pressure and extract cleanliness with the active settlement state, the live `Operation Flow` tags now surface settlement volatility plus settlement-specific peel risk, and the new `retake-peel` showcase proves that a profitable Customs Quay return can still demand an early leave because the wider town remains enemy-owned. The unresolved product choice is whether that settlement-specific exfil coaching should remain a standing readability layer during unstable returns, or whether it should get quieter after players learn that broken holds make peels collapse sooner. The latest evidence is `automation-artifacts/retake-peel-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should one cleared memorial-return raid be enough to keep the finale's `Casualty corridor steadied` prep output live, or should late tuning require repeated recovery reps before that payoff remains visible?
  - `Reason`: This run changed the recovery-path threshold so a single successful body-return can seed `campaign.finale.prepOutputs.recovery-path` once the chair debt is actually cleared, then added the `recovery-corridor-payoff` showcase to isolate that state on the operations board. The unresolved product choice is whether that first memorial-return proof should remain a durable campaign milestone, or whether the corridor should quiet back down until the squad repeats the feat often enough that it feels fully institutionalized. The latest evidence is `automation-artifacts/recovery-corridor-payoff-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new settlement-side `tacticalProblem`, `tacticalPlan`, and `extractRisk` coaching stay visible in briefing, sector cards, debrief, and snapshot once return-state replayability is learned, or should some of that explicit retake guidance collapse back down to shorter settlement labels?
  - `Reason`: This run turned `map.settlement` from mostly a label/history read into a tactical return-state surface. The shared settlement read now carries one concrete battlefield problem, one route plan, and one extract consequence, those lines now show up across the briefing and stash-facing sector/debrief surfaces, and the new `territory-retake` showcase proves Customs Quay can be reviewed as mostly lost ground with only one fragile foothold still alive. The unresolved product choice is whether that explicit coaching should remain part of the settlement identity, or whether it should quiet down after players internalize how changing control states alter the route. The latest evidence is `automation-artifacts/territory-retake-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the scar-control `territory-claims` slice stay as an explicit three-state board with one live flag, one planted hold, and one dead strip visible at once, or should later runs collapse that proof into fewer but more simulation-driven return states once players internalize the reclaim language?
  - `Reason`: This run stabilized the district-return milestone's strongest authored proof instead of inventing a new settlement pocket. The `territory-claims` showcase now keeps the player inside the live flag-hold radius long enough for automation to capture and verify one breaking claim, one planted hold, and one bagged dead strip in the same camera slice, which makes the settlement-return language finally deterministic. The open product choice is whether to preserve that explicit three-state scar board as a standing readability aid, or eventually let the return-state proof grow quieter and more state-driven once the reclaim grammar is learned. The latest evidence is `automation-artifacts/territory-claims-2026-04-20.png` and `automation-artifacts/territory-claims-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new raid-side `Pressure Posture` read keep spelling out `Action`, `Window`, and `Threat` whenever a lane is bent, or should some of that coaching collapse once players internalize what a real crossing timer and reserve timer feel like?
  - `Reason`: This run deepened the existing AI-pressure proof slice instead of widening the state machine again. `map.pressurePosture` now exposes `actionLabel`, `windowLabel`, `threatLabel`, `windowSeconds`, and reserve timing alongside pinned and suppressed counts, the tactical drawer and Phaser rail both tell the player what the lane is buying right now, and `verify --id pinned-pressure` now checks for a real timed crossing window instead of only a posture enum. The unresolved product choice is whether that explicit shove grammar should remain a standing combat read whenever AI pressure flips, or whether it should quiet down after players learn the posture language. The latest evidence is `automation-artifacts/pinned-pressure-2026-04-20b.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new memorial-wall `Wall echo` line and the more human chair-handoff / debrief phrasing stay explicit in the stash aftermath surfaces, or should some of that campaign voice collapse back down once players internalize what family calls, wakes, missing bodies, and reserve swaps imply?
  - `Reason`: This run continued the rimworld-dialogue package with a third authored story pack, `after-action-voices`, instead of widening resolver logic, then pushed that same aftermath voice into the memorial wall, chair handoff board, and debrief war log so stash consequence reads less like a spreadsheet. The result makes the post-raid layer feel more like one campaign voice tied to raid memory, but it also spends visible stash copy budget on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/dialogue-aftermath-pass-2026-04-20.png` and `automation-artifacts/dialogue-aftermath-memorial-wall-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new finale `launched`, `setback`, and `won` reads stay explicit in the stash operations board, or should some of that campaign-end explanation collapse once players understand the final push loop?
  - `Reason`: This run extended `campaign.finale` beyond pure readiness by adding authored `final-stronghold-launch`, `final-stronghold-setback`, and `true-escape` states with shared `statusLabel`, `followThrough`, and `retryInstruction` copy, so the endgame no longer disappears the moment it leaves the planning wall. The new reads make launch commitment, failed assaults, and true escape materially reviewable, and `verify --id true-escape` now proves the campaign-complete closure panel, aftermath summary, and deploy lockout instead of leaving the win state screenshot-only. The remaining product choice is whether that late-campaign explanation should stay explicit long term, or whether veteran players should eventually see a quieter wall once the final-push grammar is learned. The latest evidence is `automation-artifacts/final-stronghold-setback-2026-04-20.png`, `automation-artifacts/true-escape-verify-2026-04-20.png`, and `automation-artifacts/true-escape-closure-panel-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new `Pier Trench Belt` authored shove stay as an always-available deterministic showcase, or should future runs fold that trench-lip proof into more dynamic settlement-state variations once the assault language is learned?
  - `Reason`: This run closed the clearest remaining main-map proof gap by adding a dedicated `trench-assault` showcase instead of leaving trench pressure implied through grenade pockets, coffee holds, and route copy. The Crosswind Docks slice now stages PKM lip cover, a bend frag, rear casualty insurance, and explicit lower-pier squad assignments under one authored shove, which makes the north-star trench-line milestone finally reviewable in one pass. The unresolved product choice is whether to preserve that explicit deterministic trench proof for repeated review, or to let the trench beat become more state-driven and variable once the baseline assault language is internalized. The latest evidence is `automation-artifacts/trench-assault-showcase-2026-04-20.png`.
  - `Decision owner`: Human directive needed.
- `Open`: Should the new raid-side `Combat Audio` panel stay visible whenever the lane is hot, or should that read eventually collapse once players internalize what dominant weapon family, snap-by danger, and grenade urgency sound like?
  - `Reason`: This run completed the combat-center package's missing audio pass by deriving one shared `combat.audioRead` from real tracers, suppression impacts, blast hits, hostile grenade fuse state, and the active boys order cue, then using that read both for a new Phaser `Combat Audio` panel and for a lightweight Web Audio synth mix that arms on first click/key. The panel makes the sound layer inspectable and screenshotable while the synth gives weapon families, near-miss danger, and grenade timing a real audible identity without external assets, but it also spends permanent rail space on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/combat-audio-pass-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new stronghold-side `Prep Outputs` stay explicit in the stash operations board and briefing, or should the campaign eventually collapse those reusable finale modifiers back into one readiness sentence once players internalize how prep raids change the assault?
  - `Reason`: This run completed the endgame package's missing preparation-raid interlock by turning existing frontline orders, extracted intel, territorial reps, and recovery work into four shared `campaign.finale.prepOutputs` instead of leaving the final wall driven mostly by abstract readiness counters. The operations board and briefing now explicitly name `Marked pull lane`, `Outer lane softened`, `Casualty corridor steadied`, and `Approach reads updated`, which makes the finale feel much more grounded in actual campaign work, but it also spends precious stash/briefing space on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/final-stronghold-prep-outputs-2026-04-20.png` and `automation-artifacts/final-stronghold-prep-outputs-board-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should recovered off-body guns stay visible on the stash rack as a persistent `Field Capture` choice, or should looted-weapon doctrine collapse back into debrief text once players understand the keep-versus-sell tradeoff?
  - `Reason`: This run completed the gun-doctrine package's missing `Looted Weapon Pass` proof slice by carrying one recovered battlefield gun through the shared raid summary, stash rack, CLI snapshot, and authored showcase path instead of treating enemy drops as only instant ammo refills. The remaining product choice is whether the captured-weapon tile should remain a standing part of the stash wall as a durable next-run temptation, or whether that explanation should get quieter after the player has internalized the doctrine tradeoff. The latest evidence is `automation-artifacts/field-capture-rack-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new per-boy `Squad Doctrine` blocks stay visible in the live boys cards, or should that weapon-role explanation eventually collapse back into the shared package line once players internalize how each gun contributes to the push?
  - `Reason`: This run deepened the gun-doctrine package's squad milestone by turning staged weapons into explicit jobs for each operator instead of leaving the boys package as one summary string. Snapshot now exposes `combat.squadDoctrine` plus `raid.squadMates[*].doctrine`, the weapon-doctrine board uses a concrete package payoff, and the live `Boys Command` cards now spell out roles like `Angle hold`, `Belt hold`, and `Room flood` with route-specific detail. It makes the squad mix much easier to read, but it also spends live HUD space on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/squad-doctrine-grid-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should `Black Orchard Redoubt` stay visible as a permanent campaign-end target in the stash operations board and briefing intel, or should the stronghold only surface once the squad has fully cleared the readiness gate with no unresolved chair debt?
  - `Reason`: This run added the first real endgame-read pass instead of leaving the finale only in north-star docs. Snapshot now exposes `campaign.finale`, the stash operations board and briefing both name the fictional stronghold plus brigade, and the new `final-stronghold` showcase proves humans can judge whether the current extracts, doctrine breadth, prep orders, and squad debt justify talking about the final offensive yet. The unresolved product choice is whether that named stronghold should stay on the wall as an aspirational long-term target, or whether it should remain quieter until the squad is genuinely clean enough to launch it. The latest evidence is `automation-artifacts/final-stronghold-operations-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Bastion AMR` stay a rare late-war doctrine pick that only arrives once the campaign is clearly leaning toward endgame prep, or should anti-materiel doctrine unlock as soon as the player can afford the brutal fee?
  - `Reason`: This run completed the gun-doctrine package's missing endgame-class milestone by adding `Bastion AMR` as a real shared weapon instead of a one-off gimmick. The stash, doctrine board, squad-command reads, and snapshot now treat it as a first-class anti-materiel rifle built to break plated lane anchors and long repeeks on `Broken Signal`, while its failure space stays fast room collapses, reload traps, and movement tax. The remaining human choice is how quickly that doctrine should enter the economy: early access makes the long-lane mastery curve visible sooner, while a delayed unlock preserves rifle and PKM scarcity for longer. The latest evidence is `automation-artifacts/endgame-amr-doctrine-card-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `AMR counter lane` coaching stay explicit whenever `Broken Signal` turns into a plated repeek problem, or should that anchor-break language quiet down once players internalize the Bastion shot timing?
  - `Reason`: This run promoted the `Bastion AMR` from a stash-side doctrine proof into a live raid-side north-star slice instead of stopping at the weapon board. `amr-counter-lane` now stages `Broken Signal` as a focused `Relay counter lane` firefight, `Pressure Posture` explicitly says `Break the plated anchor first`, `Operation Flow` treats the first AMR shot as the live commitment problem, and `verify --id amr-counter-lane` proves the boys stay on an `Anchor-break package` while the lane is still recoverable. The unresolved product choice is whether that explicit plate-break coaching should remain a standing readability layer for anti-materiel doctrine, or whether veteran players should eventually infer the same answer from the relay lane, plated anchor, and repeek timing alone. The latest evidence is `automation-artifacts/amr-counter-lane-check-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Greyline Dead Zone` district/settlement framing stay visible in briefing, stash sector cards, debrief, and tactical-drawer tags, or should some of that naming collapse once players internalize the ground?
  - `Reason`: This run used the main-map and AI package plans to derive one shared district read, one settlement-state read, one active sub-zone callout, and one pressure-posture summary from the existing route/frontline scaffold instead of inventing a parallel sim. The result makes it much clearer that the current routes are part of one replayable battlefield and that the same ground can be stable, fragile, breaking, or reclaiming on return, but it also spends persistent UI budget on labels that veteran players may eventually memorize. The latest evidence is `automation-artifacts/territory-return-district-state-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Civic-9 Sidearm` always auto-convert a no-primary deploy into a sidearm-only recovery plan, or should holstered sidearms stay reserve-only unless the player explicitly stages that survival route?
  - `Reason`: This run landed the missing handgun milestone by adding a real pistol weapon profile, holster-backed stash staging, and a `handgun-recovery` showcase that proves a sidearm-only insert on `Sundered Run` as a desperate survival doctrine instead of a new primary ladder winner. Snapshot now exposes the sidearm as a real `options.weapons` entry, authored stash state can classify it as a `deployable-sidearm`, and the doctrine board explicitly frames it as `Wrong tool` with close-quarters-only best space and catastrophic open-lane failure space. The latest evidence is `automation-artifacts/handgun-recovery-card-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new shared `Operational Wall` classification board stay visible as a permanent operator/debrief surface, or should it eventually collapse once players internalize what on the wall is deployable gear, support stock, broker weight, and unresolved memorial debt?
  - `Reason`: This run added one shared stash-normalization read across the operator tab, debrief, and CLI snapshot instead of leaving the stash meaning split across rack props, cadence copy, and memorial widgets. The board now counts deployable weapons and supplies, support tokens, broker-facing haul, and memorial debt, while snapshot exposes the same categories through `stash.items[*].category`, `stash.squadReadiness`, `stash.replacementSeats`, `stash.memorialDebt`, and `stash.operationalWall`. It makes next-run pressure much clearer, but it also spends persistent stash/debrief budget on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/debrief-operational-wall-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `PKM Support Gun` stay available in the early stash ladder as soon as the player can afford one, or should it unlock later so the hardcore opening leans longer on rifle/SMG/shotgun scarcity before belt-fed suppression enters the doctrine mix?
  - `Reason`: This run added the missing PKM-class weapon to the shared weapon framework, made the support gunner actually default to a PKM, and turned the `weapon-doctrine` showcase into a Broken Signal support-lane plan built around belt-fed pressure. It lands the north star's `PKM changes how crossings and lane control are solved` rule, but it also raises an economy-tuning choice: keeping the gun immediately purchasable makes support doctrine visible sooner, while delaying it would preserve a harsher low-kit opening for longer. The latest evidence is `automation-artifacts/pkm-weapon-doctrine-card-2026-04-20.png` and `automation-artifacts/pkm-weapon-doctrine-full-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new extract-comparison board stay visible as a permanent live `Extract Decision` surface, or should it eventually collapse to the focused lane once players internalize how different exfils trade cleanliness, hold time, and support posture?
  - `Reason`: This run turned the existing extract-choice strip into a real comparison board. Every exfil now shows projected cleanliness, pressure, peel distance, hold time, squad/support posture, and whether it is cleaner or hotter than the current pull, and the new `extract-clean` / `extract-collapse` showcases exist so humans can judge whether this level of explicit doctrine earns its persistent HUD space. It makes focused-extract planning much clearer, but it also spends rare live-board budget on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/extract-choice-comparison-2026-04-19.png`, `automation-artifacts/extract-clean-2026-04-19.png`, and `automation-artifacts/extract-collapse-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new shared `Sector Order` read stay visible as a permanent briefing intel cell plus live raid board, or should it quiet down once players already internalize how queued frontline operations change the next push?
  - `Reason`: This run turned queued frontline operations from mostly stash-side planning copy into one shared read that now survives across the whole loop. The mission briefing gets a dedicated `Sector Order` intel cell, the raid tactical drawer gets a dedicated `Sector Order` board, the Phaser right rail mirrors the same read during live raids, and snapshot now exposes the live order title, effect, and deploy instruction directly. It makes stash planning feel materially present once the raid starts, but it also spends scarce HUD and briefing budget on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/briefing-sector-order-2026-04-19.png`, `automation-artifacts/frontline-operation-live-2026-04-19.png`, and `automation-artifacts/frontline-operation-panel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the debrief-side `Chair Handoff` and `Replacement Queue` stay visible as permanent after-action surfaces, or should they collapse once players understand that rites, missing bodies, reserve depth, and rookie attachments gate who can actually take the next seat?
  - `Reason`: This run pulled the existing stash-only chair-turn and replacement logic into the immediate debrief, so aftermath now names which chair is blocked by a family call or wake, who the cleanest reserve or rookie handoff is, and whether the seat can actually turn before the next push. It makes stash consequence feel more immediate after extract, but it also spends scarce debrief space on explanation that veterans may internalize after a few losses. The latest evidence is `automation-artifacts/debrief-chair-handoff-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new stash-side `Manifest Routing` board stay visible as a permanent debrief intake wall, or should it collapse once players already understand where route intel, clinic stock, ammo lots, and breaker metal go after extract?
  - `Reason`: This run turned extracted contraband from a single debrief sentence into a dedicated four-card routing surface. The debrief now names count, destination, and next-push meaning for intel, sealed medical haul, ammo lots, and breaker metal, with the currently demanded category getting a stronger paid-first treatment so the stash reads like a real intake wall instead of flavor text. It clarifies how haul becomes continuity, but it also spends more persistent debrief budget on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/debrief-manifest-routing-2026-04-19.png` and `automation-artifacts/debrief-manifest-routing-panel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new shared `Weapon Doctrine` board stay visible as a persistent stash loadout card and raid tactical-drawer panel, or should one side quiet down once players learn the weapon-role language?
  - `Reason`: This run turned gun doctrine from short fit strings into a dedicated player-facing read that now spells out route fit, best space, failure space, boys package, and the current route call in both the stash loadout column and the raid tactical drawer. It makes the north star's `bring the right gun for the operation` rule much clearer, but it also spends ongoing UI budget on explanation that some players may only need until the weapon ladder is internalized. The latest evidence is `automation-artifacts/weapon-doctrine-card-2026-04-19.png` and `automation-artifacts/weapon-doctrine-panel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new raid-side `Combat Pulse` read stay visible whenever the tactical drawer is open and the Phaser rail sees live command effects, or should it collapse until there is an active tactical action or heavier effect traffic?
  - `Reason`: This run turned the new combat-center snapshot fields into an actual player-facing read instead of leaving tracer, impact, grenade, and selected-boy action state mostly inside automation JSON. The tactical drawer now has a dedicated `Combat Pulse` board, the Phaser raid rail now mirrors it as a scene panel, and the new `combat-presentation` showcase stages a live frag lane so humans can judge whether this command-readability surface earns its HUD budget outside showcase review. The latest evidence is `automation-artifacts/combat-pulse-panel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the heavier per-weapon muzzle, tracer, and impact treatment stay this explicit in live raids, or should late tuning pull some of that intensity back once PKM/AMR lane identity is internalized?
  - `Reason`: This run used the combat-center package's VFX milestone to push weapon family identity into the shared frontline effect path instead of inventing one-off spectacle. `FrontlineImpactState` now carries optional `weaponId`, the Phaser raid scene draws family-specific muzzle halos, tracer bodies, impact sparks, and suppression rings, and the authored `combat-presentation` showcase now reads more clearly when PKM/AMR pressure is on the lane. The unresolved product choice is whether this stronger heavy-weapon read should remain part of the game's visual identity, or whether future tuning should quiet it down after the doctrine ladder is learned. The latest evidence is `automation-artifacts/combat-vfx-pass-2026-04-20.png` and `automation-artifacts/combat-vfx-panel-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new shared `Operation Flow` read stay visible as a permanent tactical-drawer and debrief classifier, or should it quiet down once players already understand the run arc from extract pressure, casualty state, and stash fallout alone?
  - `Reason`: This run formalized the extraction package's missing operation grammar by deriving one shared read from route noise, chosen exfil, crash-wave readiness, recovery burden, and ring state. Snapshot now exposes the raid phase and exit intent directly, the tactical drawer gets a dedicated `Operation Flow` board, the Phaser extract rail mirrors the same arc, and stash debrief now classifies the last pull as disciplined, greedy, forced, or recovery-driven. It makes the leave-or-stay wrapper much clearer, but it also spends visible HUD and debrief space on explanation that veteran players may eventually internalize. The latest evidence is `automation-artifacts/operation-flow-panel-2026-04-19.png` and `automation-artifacts/debrief-operation-summary-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the stash-side `Hard Start` board stay visible whenever the operator is unarmed, underfunded, or leaning on better-armed boys, or should it only surface during explicit low-start showcases and genuinely critical edge cases?
  - `Reason`: This run turned the north star's `poor conscript / weak gear / almost-empty stash` opening into an actual loadout-column card instead of leaving it buried in stash counts and the CLI snapshot. The new `hardcore-start` showcase now stages no primary, zero field sustain, a thin credit cushion, and a boys-edge read in one focused board, which makes the intended opening fantasy much clearer but also spends visible stash HUD budget on coaching that some players may only need early on. The latest evidence is `automation-artifacts/hardcore-start-board-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the mission briefing keep this new explicit tactical slate with threat, weapon fit, load, and extract readouts visible on every deploy, or should it quiet down again once players already know the route language?
  - `Reason`: This run kept the controls-first briefing direction, but stopped wasting the richer beat payload already in `src/main.ts` by turning it into a compact deploy slate. The overlay now surfaces the active beat headline/detail plus four small route reads before the raid starts, which makes deployment feel more like a live tactical brief instead of a generic controls screen, but it also spends more pre-raid UI budget on information that veteran players may eventually internalize. The latest evidence is `automation-artifacts/briefing-tactical-slate-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new explicit boys-loadout wedge stay visible in the main raid surfaces, or quiet down once players already understand that stash-side squad weapons materially change how the escort fights?
  - `Reason`: This run stopped the named escort from simply mirroring the player's gun and instead made the live wedge blend the staged squadmate loadout into one shared combat profile. The DOM `Boys Wedge`, frontline support cards, and Phaser `BOYS NET` panel now all spell out the current rifle/SMG mix so players can feel that squad weapon planning matters before the first breach, but that extra explanation also spends scarce HUD budget on information that might later become implicit once the system is learned. The latest evidence is `automation-artifacts/boys-command-loadout-wedge-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `territory-claims` scar-belt presentation stay this explicit in regular raids, or quiet down once players already understand how planted holds, live flag teams, and dead strips read in the lane?
  - `Reason`: This run turned route-flag and body-recovery control states into a fully authored battlefield slice instead of mostly rings, labels, and side-panel wording. The new `territory-claims` showcase pulls one planted hold, one live break, and one wiped scar into the same Crosswind Docks camera view, then adds sandbags, route boards, flare-lit flag work, torn Blue pennants, and drag marks directly into the Phaser frame so players can read scar control at a glance. It lands the `we took this ground, we are losing this ground, and this ground only remembers the dead` fantasy much harder, but it also spends more visual budget on a system that might later work as subtler battlefield language once the player has learned it. The latest evidence is `automation-artifacts/territory-claims-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `white-van-ambush` logistics-hit dressing stay this explicit in regular raids, or quiet down once players already understand that a civilian shell with a ripped tailgate, dropped mast tube, and Blue recovery chatter means valuable comms gear is hanging in the lane?
  - `Reason`: The slice now goes beyond visual dressing and reads through the shared raid grammar too. `getPressurePostureRead` explicitly tells the player to `Strip the mast kit before reserve`, `getRaidOperationRead` treats the white van as a live comms-strip payday instead of generic commitment copy, the squad doctrine pivots into a dedicated `Comms strip package`, and `verify --id white-van-ambush` now proves the convoy hit, doctrine surface, strip posture, and exposed drop cache all stay aligned. The unresolved product choice is whether that explicit logistics-strip coaching should remain part of the raid's readability identity long term, or whether veteran players should eventually infer the same short theft window from the open tailgate, exposed mast kit, and recovery timer alone. The latest evidence is `automation-artifacts/white-van-ambush-2026-04-20.png` and `automation-artifacts/white-van-ambush-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Comms strip package` / `Strip the mast kit before reserve` coaching stay explicit whenever a white-van logistics hit is still salvageable, or should that raid-side doctrine language quiet down once players internalize the relay-strip pattern?
  - `Reason`: This run closed the white-van slice's shared-grammar gap instead of leaving the comms ambush as a strong screenshot with generic tactical reads. `white-van-ambush` now drives `Pressure Posture`, `Operation Flow`, and the boys board through one coherent comms-strip problem, `verify --id white-van-ambush` proves the relay verge stays on `Comms strip package`, and the remaining product choice is whether that explicit strip-the-mast coaching should remain part of the raid identity long term or quiet down once veteran players learn the pattern. The latest evidence is `automation-artifacts/white-van-ambush-2026-04-20.png` and `automation-artifacts/white-van-ambush-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `caravan-trap` road-war dressing stay this explicit in regular raids, or quiet down once players already understand that piled wrecks, cooking ammo, and Blue trap chatter mean a convoy walked into an obvious kill zone?
  - `Reason`: This run turned the direction document's `16-truck armored caravan driving into an obvious trap and getting wiped` fantasy into a visible authored slice instead of leaving convoy disaster as distant flavor. The new `caravan-trap` showcase stages a piled wreck lane on `Sundered Run`, loose haul, one hanging body bag, active Blue trap chatter, and a dedicated `CARAVAN TRAP` / `AMMO COOKOFF` panel in the Phaser frame, which lands the living-frontline massacre fantasy much harder but also spends visible world-space and HUD budget on a very explicit road-war read. The latest evidence is `automation-artifacts/caravan-trap-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Ramp crash package` / `Crash the ramp before dismount` coaching stay explicit whenever an armored troop drop is still spilling onto the relay berm, or should that raid-side language quiet down once players internalize the pattern?
  - `Reason`: This run promoted `armored-drop` from a visible set piece into a first-class shared-grammar proof. `getPressurePostureRead` now explicitly tells the player to `Crash the ramp before dismount`, `getRaidOperationRead` treats the wagon as the live reinforcement-break problem instead of generic commitment copy, the boys flip into a dedicated `Ramp crash package`, and `verify --id armored-drop` proves the relay berm, ramp crisis, and squad doctrine all stay aligned. The unresolved product choice is whether that explicit troop-drop coaching should remain part of the raid identity long term, or whether veteran players should eventually infer the same short crash window from the open ramp, dismount spill, and berm pressure alone. The latest evidence is `automation-artifacts/armored-drop-2026-04-20.png` and `automation-artifacts/armored-drop-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new drone-sweep coaching stay explicit whenever a live spotter eye and route board create a short theft-and-peel window, or should that language quiet down once players internalize the pattern?
  - `Reason`: This run promoted `drone-sweep` from a screenshot-only infiltration beat into a first-class shared-grammar proof. `getPressurePostureRead` now explicitly tells the player to `Stay low until the board is bagged`, `getRaidOperationRead` treats `Relay cut sweep` as a live `spotter-eye cut` instead of generic commitment copy, the boys flip into a dedicated `Drone slip package`, and `verify --id drone-sweep` proves the hedge hold, board rush, and peel window stay aligned. The unresolved product choice is whether that explicit route-board coaching should remain part of the raid identity long term, or whether veteran players should eventually infer the same theft window from the buzzing eye, tripod board, and low-profile squad posture alone. The latest evidence is `automation-artifacts/drone-sweep-2026-04-20.png` and `automation-artifacts/drone-sweep-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `shed-hide` presentation stay this explicit in regular raids, or quiet down once players already understand that a slit-cut shed with whispered boys traffic means a live hide-and-let-the-sweep-pass window?
  - `Reason`: This run turned the direction document's `solo hiding in a shed while enemy clears the village above you` fantasy into a visible infiltration slice instead of leaving quiet survival mostly inside noise pressure numbers. The new authored `shed-hide` showcase reuses the bunker incident path to stage a slit-cut tin shed, blackout cloth, hidden ammo wrap, dead radio, whispered boys assignments, live Blue sweep chatter, and a dedicated `SHED HIDE` / `HIDE WINDOW LIVE` panel in the Phaser frame, which lands the quiet-under-pressure fantasy much harder but also spends visible HUD and playfield budget on a beat that may eventually work better as subtler environmental language. The latest evidence is `automation-artifacts/shed-hide-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new armored troop-drop dressing stay this explicit in regular raids, or quiet down once players already understand that a rear-ramp wagon plus mixed tape dismounts means a live reinforced push?
  - `Reason`: This run turned the direction document's `armored vehicles rolling in, dropping troops after being spotted` fantasy into a visible authored raid slice instead of leaving armored response as abstract pressure. The new `armored-drop` showcase stages a Blue drop wagon with the rear ramp down, explicit blue/green/yellow dismount silhouettes, hostile ramp-chief chatter, and a dedicated `ARMORED DROP` / `TROOPS DISEMBARKING` panel in the Phaser frame, which lands the living-frontline escalation much harder but also spends visible world-space and HUD budget in an already busy relay fight. The latest evidence is `automation-artifacts/armored-drop-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new stash-side chair handoff board stay this explicit in regular use, or quiet down once players already understand that memorial rites directly gate who inherits the next squad seat?
  - `Reason`: This run added a dedicated `Chair Handoff` board to the operator tab so the stash now explicitly bridges memorial grief and roster replacement instead of making players mentally reconcile separate panels. The board names which dead operator's chair is blocked by an unanswered family call or unpaid wake, shows which reserve or rookie is waiting behind that seat, and makes the squad consequence loop much easier to read at a glance, but it also adds another persistent stash-side panel to an already information-dense operator surface. The latest evidence is `automation-artifacts/chair-handoff-board-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new old-hunter search dressing stay this explicit in regular raids, or quiet down once players already understand that a lantern, shack, and route board mean a live human search beat?
  - `Reason`: This run turned the direction document's `old hunter looking for his missing son in the zone` fantasy into a visible raid slice instead of leaving it as flavor text. The new authored `hunter-search` showcase repurposes the civilian incident into a relay-treeline rescue with a shack, lantern, search board, old hunter silhouette, route-note clue, and a dedicated `HUNTER SEARCH` / `HUNTER MOVING` board in the Phaser frame, which lands the human cost of the frontline much harder but also spends visible panel and world-space budget in an already busy raid. The latest evidence is `automation-artifacts/hunter-search-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the stash memorial wall keep the new explicit family-call and wake-owed steps visible in regular use, or quiet down after players already understand how the loss loop works?
  - `Reason`: This run turned squad loss follow-through into a visible stash feature instead of a mostly plain memorial list. The new authored `memorial-wall` showcase stages one family-pending death and one wake-pending death, memorial cards now surface the last radio line plus the next rite to perform, and the wall makes the replacement queue feel tied to actual grief instead of a detached roster swap. The latest evidence is `automation-artifacts/memorial-wall-panel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new armored-evac dressing stay this explicit in regular raids, or quiet down once players already understand that a rear-ramp wagon plus shouted Blue evac traffic means a live wounded pull?
  - `Reason`: This run turned casualty recovery into a visible enemy medevac fight instead of only a drag lane. The new authored `armored-evac` slice stages an armored wagon with the rear ramp down beside the dead strip, adds shouted Blue evac traffic, and promotes the lower-left raid board to `ARMORED EVAC LIVE` / `MEDEVAC WINDOW`, which lands the living-frontline fantasy harder but also spends more scene and HUD budget in an already busy recovery pocket. The latest evidence is `automation-artifacts/armored-evac-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new surrender-window dressing stay this explicit in regular raids, or quiet down once players already understand that a routed pocket with raised hands and a route-note bag is a live prisoner-taking beat?
  - `Reason`: This run turned the authored `surrender-window` slice from mostly routed-pocket text into a visible war beat in the Phaser raid frame. Routed firefights that have broken into surrender now get a dedicated `SURRENDER WINDOW` / `SURRENDER LOCK LIVE` panel plus hands-up silhouettes, a route-note bag, a planted-flag read, and a visible lock ring around the scar, which makes the `hold them at gunpoint, bag the route intel, and lock the pocket` fantasy much clearer but also spends visible panel and world-space budget during already busy dock fights. The latest evidence is `automation-artifacts/surrender-window-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the planned extract support stack stay this explicit with named civilian cars, ladder pulls, and signal-van dressing in regular raids, or quiet back down once players already understand the peel?
  - `Reason`: This run turned the authored `extract-pressure` slice from mostly world markers into a visible convoy peel. The focused pull now stages `Lada screen`, `Moskvitch cut`, `Ladder casualty pull`, and `Signal van` as actual in-scene support dressing around the beacon, and the raid-side `Extract Pressure` panel now names those supports directly. The extraction fantasy is stronger because the route finally reads like `me and the boys` peeling out with improvised civilian transport and recovery traffic instead of abstract exfil geometry, but the added cars, van, and support copy also spend visible playfield and HUD budget during already busy late-raid pulls. The latest evidence is `automation-artifacts/extract-pressure-convoy-peel-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should bunker footholds keep the new wall-screen movie flicker and seated boys silhouette read, or quiet back down once players already understand that a laptop-lit concrete pocket means a real reset window?
  - `Reason`: This run pushed bunker footholds past generic set dressing by giving focused bunker incidents their own lower-left raid panel state plus a stronger in-scene lived-in read: the bunker now shows a wall-screen flicker, seated watchers, rifle lean, laptop glow, and calmer copy about the boys catching one beat of concrete calm before the next shove. The fantasy payoff is stronger because the raid finally reads like the squad is actually hunkering in a bunker instead of only looting one, but the extra panel and scene dressing also spend visible playfield budget in an already busy district. The latest evidence is `automation-artifacts/bunker-foothold-movie-hold-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should frag grenades stay as a lightweight fixed raid allotment, or should the next stash pass turn them into real kit that consumes pocket space and must be reserved before deploy?
  - `Reason`: This run added a shared grenade lane to the first playable raid loop: the player now has a visible frag stock with a dedicated `G` throw input, nearby hostiles can also arc grenades into hot holds, the HUD tracks stock plus cooldown, and the new `grenade-pocket` showcase proves blast pressure directly in the Phaser frame. The current implementation keeps grenades intentionally lightweight at raid start so the combat payoff lands immediately, but the open product choice is whether explosives should remain a simple per-raid tactical resource or become part of the deeper stash-and-loadout economy the project already wants. The latest evidence is `automation-artifacts/grenade-pocket-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new civilian-shelter evacuation dressing stay this explicit in regular raids, or quiet down once players already understand that a cellar lamp plus white van means a live family escort window?
  - `Reason`: This run turned the `civilian-window` slice into a visible under-fire evacuation beat instead of a lighter shelter prompt. The staged relay block now seats the player on an active family escort with a white curb van, live evac timer, escort screen, cellar lamp, and a dedicated civilian read in the Phaser frame, which lands the "protect civilians while the war keeps moving" fantasy much harder but also spends visible playfield and HUD budget in an already busy raid. The latest evidence is `automation-artifacts/civilian-window-escort-column-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new convoy ambush dressing stay this explicit in regular raids, or quiet back down once players already understand that a hung truck and loose pallet mean a live strip window?
  - `Reason`: This run turned the existing `frontline-supply` incident into a visible convoy ambush beat instead of mostly a logistics read in text and icons. The staged `Sundered Run` slice now seats the player on a real relief-cut kill lane with hung trucks, a loose ammo pallet, flare smoke, an active strip timer, and a stronger escort screen in the Phaser frame, which lands the living-war logistics fantasy much harder but also spends more world-space and contrast budget in a route that is already visually busy. The latest evidence is `automation-artifacts/frontline-supply-convoy-ambush-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new body-recovery dressing stay this explicit in regular raids, or quiet down after players already understand what a dead-strip pull looks like?
  - `Reason`: This run turned casualty recovery into a dedicated authored raid beat instead of leaving it mostly in simulation copy. The new `body-recovery` showcase now stages a live wounded drag, black bag, aid satchel, drag straps, recovery pulse, and a dedicated lower-left recovery read in the Phaser frame, which lands the "me and the boys pull our dead out under fire" fantasy much harder but also spends visible playfield budget in a crowded dock fight. The latest evidence is `automation-artifacts/body-recovery-showcase-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should Blue/Green/Yellow tape armbands stay this saturated on hostile bodies during live raids, or should they settle down after players already learn how to read the tape factions?
  - `Reason`: This run kept the hostile combatant foundation shared but split presentation by tape color in the Phaser raid scene, so Blue, Green, and Yellow enemies no longer all read as the same blue-marked operator. The readability win is real in dense room fights and mixed lane pushes, but the stronger armband contrast also spends some visual budget in already busy top-down stacks. The latest evidence is `automation-artifacts/tape-armbands-room-clear-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new in-scene `Blue Shouts` panel and world shout marker stay pinned whenever hostile chatter is active, or collapse back once players already understand which tape is calling the lane?
  - `Reason`: This run pushed the existing hostile lane-chatter system out of the DOM-only layer and into the Phaser raid view with a dedicated `BLUE SHOUTS` panel plus a matching world marker near the active hot pocket. The district now reads much more like two sides are arguing over the same cut in real time, but the new panel and marker also spend visible playfield budget beside the tactical map and existing left-rail combat boards. The latest evidence is `automation-artifacts/hostile-lane-chatter-scene-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should bunker footholds keep the new in-scene laptop, cups, taped-mag stack, and calm pulse dressing, or should that presentation quiet down after players already learn what a concrete reset pocket means?
  - `Reason`: This run turned the bunker foothold from mostly copy plus an icon into a visible lived-in raid state. The Phaser scene now dresses the pocket with bunker slab geometry, warm laptop glow, hot cups, taped mags, and a stronger settle pulse during the live hold window, which lands the "me and the boys pause in concrete before stepping back into the war" fantasy harder but also spends more world-space and contrast budget in already busy firefights. The latest evidence is `automation-artifacts/bunker-foothold-dressing-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should scar world markers stay pinned for planted flags, breaking cuts, and recovered-body lanes, or quiet down once the player has already focused that pocket?
  - `Reason`: This run extended the Phaser objective-marker layer so worked firefight scars now throw explicit `Flag Hold`, `Break`, and `Bodies` labels into the playfield instead of hiding mostly in side panels and tiny glyphs. The route reads more like a frontline with visible memory now, but the extra labels also spend world-space budget during already busy district fights. The latest evidence is `automation-artifacts/territory-claims-after-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Blind Fire` read stay pinned inside `Noise Discipline` whenever nearby fighters are spraying from cover, or collapse back into the wider war/noise copy once players already understand the lane?
  - `Reason`: This run added a shared blind-fire layer so suppressed Blue bodies, frontline fireteams, and active firefights can all keep spraying low-confidence bursts from cover instead of going visually dead under pressure. The same state now surfaces in the DOM `Noise Discipline` card and Phaser noise panel as an explicit `Blind Fire` read, which lands the trench-style suppression fantasy harder but also spends extra HUD text budget during already busy hot lanes. The latest evidence is `automation-artifacts/blind-fire-noise-discipline-2026-04-19.png` and `automation-artifacts/blind-fire-noise-card-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new hunkered suppression poses stay this loud in regular raids, or settle back once players already understand when Blue is pinned versus still pushing?
  - `Reason`: This run added shared pressure-aware body poses in the raid scene, so suppressed and pinned Blue operators now hunker lower, staggered units wobble, pushing bodies lean forward, and the same pose language also bends frontline friendlies plus the boys wedge. The firefight reads closer to the direction document's suppressive-fire fantasy now, but these stronger posture silhouettes also spend more visual budget during already busy extract stacks. The latest evidence is `automation-artifacts/suppression-pose-extract-pressure-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new hostile `Blue Shouts` chatter stay as its own persistent raid surface, or collapse into the wider traffic stack once players learn how to read the lane?
  - `Reason`: This run added a hostile lane-chatter layer driven by the same live raid context as the boys net, so nearby Blue/Green/Yellow tape forces now shout their own lane orders during quiet holds, contact spikes, civilian beats, surrender pressure, and extract crashes. The fiction is stronger because the route now feels like two sides arguing over the same ground instead of only the player's squad talking into silence, but the added `Blue Shout` card and `Blue Shouts` log also spend more HUD budget during already text-heavy raids. The latest evidence is `automation-artifacts/hostile-lane-chatter-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new material-aware bullet impact bursts stay loud in regular raids, or quiet down once the player has learned the difference between dirt puffs and hard-surface strikes?
  - `Reason`: This run pushed ballistic collisions through a shared impact-material layer so open-ground misses now kick soft dust while wall and room strikes break into sharper concrete flashes, and the same read applies to player shots, hostile rounds, and frontline tracer impacts. The firefights read more like a real lane under pressure now, but the added bursts also spend more visual budget in already busy room stacks. The latest evidence is `automation-artifacts/material-impact-room-clear-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `Noise Discipline` read stay pinned in the tactical drawer from raid start, or should it stay quiet until the lane is at least `Heard` or one sweep away from blowing open?
  - `Reason`: This run added a shared `getNoisePressureRead` layer plus a dedicated tactical-drawer `Noise Discipline` card and matching Phaser panel so the player can read quiet lane versus compromised lane before reinforcements materialize. The clarity win is real for the infiltration fantasy, but the new always-available card also spends persistent HUD budget in every raid instead of only surfacing when the route is meaningfully close to escalation. The latest evidence is `automation-artifacts/noise-discipline-full-2026-04-19.png` and `automation-artifacts/noise-discipline-card-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should bunker footholds stay as explicit calm-life beats with named concrete reset copy, or collapse back into terser generic support text once players already understand the payoff?
  - `Reason`: This run added a shared `bunker` frontline incident that lets the squad settle into a basement hold for a taped-mag reset, route-sketch intel, and a calmer shove window. The new beat finally lands the "me and the boys pause in concrete before stepping back into the war" fantasy, but it spends more HUD and comms budget than a generic support marker. The latest evidence is `automation-artifacts/bunker-foothold-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new crash-stack read stay explicit about incoming composition (`1 rusher`, `2 rushers + rifle screen`) in the raid HUD, or collapse back to shorter generic timer-only flare warnings after players learn the system?
  - `Reason`: This run added a shared reinforcement-pressure read that now feeds the DOM `Extract Decision` card, threat/status copy, CLI snapshot, Phaser `Extract Pressure` panel, and the lead world marker. The living-frontline pressure reads much more like a specific force stepping onto the lane instead of an abstract countdown, but it also spends more HUD text budget during already tense extract moments. The latest evidence is `automation-artifacts/reinforcement-pressure-full-2026-04-19.png` and `automation-artifacts/reinforcement-pressure-card-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new squad-strain read stay always visible on every named-boy raid card, or only expand once the wedge is actually running hot?
  - `Reason`: This run added a shared squad-strain layer powered by live escort ammo, hostile distance, sustain cover, and extract pressure, then pushed it into the DOM `Boys Command` roster/cards plus the Phaser `BOYS NET` panel. The readability win is real because the player can now see which named boy is closest to breaking before extraction, but the new always-on labels spend more text budget in the raid drawer. The latest evidence is `automation-artifacts/squad-strain-boys-command-2026-04-19.png` and `automation-artifacts/squad-strain-extract-pressure-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the boys' new voice signatures stay as short identity tags and flavored tactical barks, or should the next squad pass spend more HUD/log budget on fuller RimWorld-style personality chatter?
  - `Reason`: This run pushed `voiceTag` and `voiceSignature` identity through the shared squad roster, live squadmate state, squad comms, and squad log so Rook, Makar, Yara, and incoming recruits now read as specific personalities in the stash roster, DOM `Boys Command` card, Phaser `Boys Net`, and `NET TRAFFIC` instead of only as role labels. The ambient comms templates also pick up speaker-specific suffixes (`Steady hand`, `Loud gun`, `Dry medic`) so the war-story texture is stronger, but the current implementation still stays relatively terse for top-down readability. The latest evidence is `automation-artifacts/boys-voice-net-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: When a route-flag scar is half-held or newly planted, should the boys net stay dominated by claim language until that pocket resolves, or should convoy/civilian/extract beats steal the squad read back sooner?
  - `Reason`: This run pushed route flags and body recovery into the same shared squad-context layer that already powers civilians, surrender, breach, and exfil, so the DOM `Boys Command` card and Phaser `Boys Net` panel now switch into explicit `Lock The Scar`, `Hold The Flag`, and dead-strip recovery reads instead of staying on generic escort chatter. The new direction lands the planted-flag fantasy much harder, but it also means claim pressure can temporarily outrank other war beats in the squad voice. The latest evidence is `automation-artifacts/territory-claims-after-2026-04-19.png` and `automation-artifacts/territory-claims-squad-command-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the planned-exfil support stack keep naming the actual peeling support elements (`Relay cut`, `Signal van`, `Ladder casualty pull`) in the raid HUD, or collapse back to terse generic tags once the player already understands the pull?
  - `Reason`: This run changed the shared planned-extract cue layer so the pre-beacon peel no longer reads as anonymous `FIRST TEAM` / `RECOVERY` placeholders. The `Boys Wedge` payoff card, extract selector cards, and world markers now carry the actual support labels bending onto the chosen exit, which makes the living-frontline extraction fantasy stronger but also spends more HUD text budget. The latest evidence is `automation-artifacts/extract-stage-named-supports-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Resolved`: Let pre-raid stash planning directly cycle each named boy's raid weapon instead of keeping squad kits fixed to role text only.
  - `Reason`: The stash `Squad Roster` cards now expose per-boy `Prev Kit` / `Next Kit` controls, and the selected rifle/SMG/shotgun choice is carried back into the shared squad roster state that spawns the live wedge in raids. This keeps the shared-combatant directive intact while finally making squad kit planning a visible stash verb instead of a hidden role default. The latest evidence is `automation-artifacts/squad-loadout-planning-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should squadmate weapon swaps stay as free role-level planning, or should later stash economy passes require actual reserved guns and ammo burden to arm each boy?
  - `Reason`: The new stash-side squad kit planner makes the boys feel much more like a real deployable team, but it currently treats their rifle/SMG/shotgun choice as tactical planning rather than consuming physical stash inventory. The next human choice is whether the current lightweight planning abstraction should stay for readability, or whether squad loadouts should eventually compete for concrete stash guns, magazines, and deployment cost.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new active war-beat action markers stay loud and two-line while a convoy/civilian/surrender/body-recovery action is live, or collapse back to shorter labels once the player has already focused that pocket?
  - `Reason`: This run added a shared `getFrontlineIncidentActionRead` layer and wired it into the DOM `War Beat Focus` card, the interaction prompt, and the Phaser playfield, so live actions now read as explicit tasks like `Walk out relay cellar family / EVAC 1.4s` and `Lock cargo crossfire / LOCK 1.3s` instead of generic opportunity text. The remaining product choice is whether that extra verbosity should keep dominating the objective-label budget for maximum clarity during tense moments, or whether the marker should shrink after focus is established to preserve more screen quiet. The latest evidence is `automation-artifacts/civilian-action-read-2026-04-19.png` and `automation-artifacts/surrender-action-read-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should civilian shelters and surrender pockets keep owning the boys net all the way through their active hold, or should squad chatter fall back to generic contact once the shooting fully takes over?
  - `Reason`: This run made those human beats first-class squad contexts, so the live `Boys Command` card and net traffic now flip into escort and surrender-specific assignments instead of generic contact lines. The next tuning choice is whether that thematic focus should stay pinned until the beat resolves, or whether heavy firefight pressure should be allowed to steal the boys back into broader combat chatter once the lane goes fully loud. The latest evidence is `automation-artifacts/civilian-squad-command-card-2026-04-19.png` and `automation-artifacts/surrender-squad-command-card-2026-04-19.png`.
  - `Decision owner`: Human directive needed.

- `Resolved`: Put one explicit civilian-shelter evacuation beat into the live frontline incident loop before widening civilians into a deeper population or hostage system.
  - `Reason`: The direction document already calls for civilians refusing to leave and for the player to care about keeping guns off occupied buildings, but the raid only expressed convoys, casualties, and firefights. This run extended `src/game/simulation.ts`, `src/main.ts`, and `src/game/scene/RaidScene.ts` so each route now carries one civilian shelter opportunity that can be escorted clear for a field dressing, route intel, and a slightly calmer extract read, all through the same action/focus path humans already understand. The latest evidence is `automation-artifacts/civilian-window-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should civilian shelters stay as optional humanitarian beats with modest intel/extract payoff, or should harming or ignoring them start pushing stronger reputation and squad-morale consequences into later raids?
  - `Reason`: The new shelter beat makes civilians visible and actionable in-raid, but it currently behaves as a tactical opportunity rather than a larger ethical or reputational system. The next human choice is whether civilian protection should remain a small route-shaping pressure, or whether the game should start remembering those outcomes more aggressively across stash and later deployments.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn the stash memorial wall into explicit family-call and wake follow-through before widening grief into deeper funeral or social systems.
  - `Reason`: The direction document already treats permanent loss as an emotional consequence, but the stash was still rendering dead boys as passive text. This run extended `src/game/simulation.ts` and `src/main.ts` so fallen squadmates now move through actionable `Call Family` and `Hold Wake` steps on the shared memorial wall, and unresolved rites now hard-bias the shared `Recovery Cadence` board until the stash actually deals with the loss. The latest evidence is `automation-artifacts/debrief-memorial-actions-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should unresolved memorial rites only warn and soften the next push, or should they start locking rookie attachment and other greedier stash actions until the wall is answered?
  - `Reason`: The new memorial flow already makes loss handling visible and mechanically relevant through the cadence board, but it currently stops short of gating broader stash behavior. The next human choice is whether `Call Family` / `Hold Wake` should remain strong emotional and readiness signals, or whether ignoring them should actively block greedier roster-management choices until the squad closes the loop.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn routed reclaimed firefights into an explicit surrender-secure beat before widening prisoners into a deeper capture system.
  - `Reason`: The direction document explicitly calls for shouted surrender and prisoner pressure, but the live raid still jumped straight from `routed` firefights to either a planted-flag follow-through or a body-recovery scar. This run extended the shared frontline-incident action path so a reclaimed routed pocket can now open a surrender window, letting the player force the surrender, bag route notes as `intel`, and still lock the scar through the same existing war-beat systems. The latest evidence is `automation-artifacts/surrender-window-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: When a firefight breaks into surrender, should the primary `War Beat Focus` card automatically steal focus from convoy and casualty beats until that surrender window is either taken or lost?
  - `Reason`: The new surrender-secure path adds a more human, squad-driven beat than a normal flag plant, but the HUD currently still respects the existing manual/priority focus logic. The next product choice is whether shouted surrender should behave like a rare must-see moment that temporarily overrides other war beats, or whether it should remain one option in the same player-directed focus stack.
  - `Decision owner`: Human directive needed.

- `Resolved`: Put explicit boys-break consequence directly inside the live extract read before widening extraction into more route-only heuristics.
  - `Reason`: The raid already knew stash value, response timing, and planned exfil posture, but the actual stay-vs-leave call still buried the squad cost in separate roster surfaces. This run added a shared `getExtractSquadRiskRead` layer in `src/game/simulation.ts`, routed it into the DOM `Extract Decision` card and the Phaser `Extract Pressure` panel, and verified the staged `extract-pressure` showcase with `Yara critical | 63% ready` on the chosen Flood Channel Ladder pull. The latest evidence is `automation-artifacts/extract-squad-risk-full-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Squad Risk` extract read stay mostly advisory, or should it start hard-biasing the main extract recommendation toward leaving whenever one named boy goes critical with bankable haul already secured?
  - `Reason`: The extract read now makes squad consequence visible, but the next product choice is how opinionated that visibility should be. A softer advisory keeps room for player greed and skill expression, while a harder bias would make the game more aggressively protect named squadmates once a profitable haul is already on hand.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface one live convoy, casualty lane, or breaking scar directly in the primary raid HUD before widening the living-frontline loop with more background-only simulation.
  - `Reason`: The route already simulated wider frontline beats and let the tactical drawer inspect them, but they still felt secondary because the main raid frame only kept extraction, squad command, and scar memory persistent. This run added a compact `War Beat Focus` card in `src/main.ts` and `src/styles.css`, plus a dedicated `war-beat-focus` showcase and CLI capture/focus support, so one live war beat can now stay readable and switchable in the main HUD without opening the drawer. The latest evidence is `automation-artifacts/war-beat-focus-card-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `War Beat Focus` card stay pinned to the explicitly selected frontline incident, or auto-promote convoys and casualty drags over firefights whenever those rarer beats are live?
  - `Reason`: The new card makes the wider frontline readable in the primary HUD, but the next product choice is whether that surface should behave like a stable player-picked anchor or a more opinionated director that automatically steals focus for rarer convoy/casualty opportunities even if the player last cared about a firefight scar. The answer will determine whether the HUD feels more tactical and dependable, or more cinematic and world-reactive.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface the named boys directly inside the live raid HUD before widening squad systems into deeper personality or persistence work.
  - `Reason`: The raid already had good boys-command language and a deeper tactical drawer, but the main frame still let the squad read as one abstract wedge unless the player opened secondary surfaces. This run added a compact named roster to the DOM `Boys Command` card in `src/main.ts` and `src/styles.css`, mirrored the same individual read into the Phaser `Boys Net` panel in `src/game/scene/RaidScene.ts`, and exposed `raid.squadMates` through the CLI snapshot so automation and humans can track the boys as specific people during live raids. The latest evidence is `automation-artifacts/boys-command-live-roster-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new live `Boys Command` roster stay always visible in the main HUD, or collapse down to only the hottest squadmate once the lane goes quiet?
  - `Reason`: The new roster makes `me and the boys` much easier to feel during live raids, especially in screenshots, but it also spends more permanent HUD budget on squad detail during quieter stretches. The next human choice is whether that always-on named roster is the right reinforcement for the squad fantasy, or whether the card should compress back to one hottest mate during cold movement and expand only once contact, breach pressure, or extraction heat is live.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn raid-side territory swings into a named stash-side `Claim Ledger` before widening the frontline loop with more detached meta systems.
  - `Reason`: The raid already tracked planted flags, breaking pockets, and recovered body lanes, but the stash debrief still collapsed that war memory into abstract territory counts. This run added a persisted `scarLedger` to `RaidSummary`, rendered a new `Claim Ledger` debrief board in `src/main.ts` and `src/styles.css`, and staged the debug summary so the stash now names specific scars plus the next follow-through each one implies. The latest evidence is `automation-artifacts/debrief-claim-ledger-2026-04-19.png` and `automation-artifacts/debrief-claim-panel-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Claim Ledger` stay focused only on the latest push, or should it retain a rolling multi-raid scar board until those claims are overwritten by later frontline outcomes?
  - `Reason`: The new debrief board now gives extraction a concrete memory layer, but it currently only reflects the most recent raid summary. The next human choice is whether that freshness is the right call for readability, or whether the stash should preserve a longer scar wall so the player can feel several named pushes and body recoveries stacking into a broader campaign memory.
  - `Decision owner`: Human directive needed.

- `Resolved`: Let ambient thermos pockets and burner holds speak through the boys net before widening coffee into bigger logistics systems.
  - `Reason`: Coffee pockets already granted useful momentum, but they still read mostly as hidden support-state detail unless the player happened to stand inside the hold ring or read the tactical support cards closely. This run added a shared `getFrontlineCoffeePocketRead` helper in `src/game/simulation.ts`, routed it into `War Read`, a new `Warm Hands` squad-comms context, the DOM `Boys Wedge`, the Phaser `Boys Net` panel, and the CLI snapshot, and also fixed coffee-specific prompt copy so ambient thermos pockets no longer claim to hand out `burner coffee`. The latest evidence is `automation-artifacts/field-coffee-boys-wedge-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Warm Reset` coffee read stay visible whenever a nearby pocket exists, or collapse once the player has already cashed in the momentum window unless another coffee pocket comes back on-net?
  - `Reason`: The new read makes trench coffee and thermos pockets feel like an intentional squad verb instead of buried support metadata, but it also spends more permanent HUD and comms budget during calmer stretches. The next human choice is whether the boys should keep reminding the player about the nearest coffee/reset pocket for atmosphere and route planning, or whether that panel should quiet down once the current cup has already paid out and no fresh reset is available.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn the stash debrief into a concrete next-push recommendation surface before widening the stash loop with more detached meta systems.
  - `Reason`: The debrief already exposed route fallout, squad status, and campaign memory, but it still left the player to manually translate that aftermath into the next stash-side sector order. This run added a `Next Push` board in `src/main.ts` and `src/styles.css` that picks the most urgent district, recommends one frontline operation (`Reinforce Pickets`, `Smuggle Cache`, `Mark Safe Exfil`, or `Stage Burner Coffee`) based on the actual fallout, and gives the player direct route-focus plus one-click queue actions from the debrief itself. The latest evidence is `automation-artifacts/debrief-next-push-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Next Push` board stay anchored to the most urgent district, or stay stricter about recommending only follow-up orders for the exact route the player just ran unless the wider frontline is collapsing?
  - `Reason`: The new debrief board now makes stash follow-through actionable, but it currently chooses the target district by urgency with a bias toward the just-played route rather than hard-locking to that route every time. The next human choice is whether that wider-war routing is the right call for the living-frontline fantasy, or whether debrief recommendations should stay more literal and only escalate away from the last route when another district is in obvious danger.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface queued next-push orders in the stash `Map` tab and mission briefing before widening the stash loop into more detached meta systems.
  - `Reason`: The debrief `Next Push` card could already queue a real frontline order, but once that click landed the setup mostly disappeared into brief copy strings and was easy to lose during re-gear. This run added a dedicated `Staged Order` card to the stash `Map` tab in `src/main.ts` and `src/styles.css`, and threaded the same queued-order read into the mission briefing so staged district prep now stays visible until the player deploys it. The latest evidence is `automation-artifacts/staged-order-map-proof-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Echo the queued `Staged Order` directly on the default `Gear` tab so the next push stays attached to re-gear instead of hiding on planning-only surfaces.
  - `Reason`: The previous stash pass already queued real district prep, but that follow-through still disappeared unless the player deliberately opened `Map` or the mission briefing. This run reused the same staged-order model inside a new `Next Push` card on the default gear column in `src/main.ts` and `src/styles.css`, paired it with the current squad-cadence read, added direct `Open Map` / `Check Boys` jumps, and authored a deterministic `next-push-gear` showcase so the reminder stays visible while the player is actually dressing for the next raid. The latest evidence is `automation-artifacts/gear-next-push-card-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Turn the extract-pressure read into an explicit boys-command recommendation layer before adding more extraction systems.
  - `Reason`: Multi-exfil choice and planned exfil lean were already working, but the raid still made the player mentally translate between `Extract Decision` and the separate `Boys Command` verbs. This run added a shared `getExtractCommandRecommendation` helper in `src/game/simulation.ts`, surfaced it as a dedicated `Boys Call` strip with one-tap command dispatch in `src/main.ts` and `src/styles.css`, mirrored the same recommendation into the Phaser `Extract Pressure` panel in `src/game/scene/RaidScene.ts`, and highlighted the matching command in the tactical order grid so extraction pressure now explicitly tells the player when to fire `Screen Exfil`, `Pull Eyes Off`, `Cover Me`, or `Stack Breach`. The latest evidence is `automation-artifacts/extract-order-recommendation-full-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Boys Call` recommendation stay persistent in the extract board, or only light up once the suggested command would materially change the lane?
  - `Reason`: The new layer makes extraction and squad command feel like one system, but it also adds one more always-on recommendation block to an already dense raid HUD. The next human choice is whether that explicit command coaching should stay visible all raid so the boys fantasy keeps reinforcing itself, or whether it should collapse during quieter moments and only surface when a real order window opens.
  - `Decision owner`: Human directive needed.

- `Resolved`: Picking an extract should immediately lean the boys' screen/recovery posture and world-space command cues toward that same pull instead of staying a player-only planning toggle.
  - `Reason`: The prior run made multi-exfil routes legible, but the selected pull still behaved too much like solo planning because the squad and world cue layer stayed quiet unless the player explicitly fired `Screen Exfil`. This run updated `src/game/simulation.ts` so live focused exfils now trigger squad comms plus a light friendly staging lean toward the chosen pull, while `src/main.ts` and `src/game/scene/RaidScene.ts` now surface a shared `Planned Exfil` cue in both the DOM `Boys Wedge` card and the Phaser `Extract Pressure` panel. The latest evidence is `automation-artifacts/extract-focus-boys-lean-2026-04-19.png`, and the staged snapshot confirms convoy/casualty incidents already flip toward the selected pull with `focusedExtractId = far-relay-spur`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should planned exfil leaning stay a light pre-beacon bias, or should the next pass let focused pulls drag more of the non-escort fireteam deeper onto the chosen exfil even while other firefights stay live?
  - `Reason`: The new pass makes extract choice feel squad-aware without auto-firing `Screen Exfil` or collapsing the rest of the district into a full fallback. The next human choice is whether that subtle planning lean is the right first step, or whether the boys should commit harder toward the selected exit before the beacon burns once the command/fallback fantasy wants more weight.
  - `Decision owner`: Human directive needed.

- `Resolved`: Focused exfils should stage visible pre-beacon recovery/fallback posture in the live HUD and scene before widening extraction work into deeper squad logic.
  - `Reason`: The prior pass made planned exfil leaning squad-aware, but it still read mostly as copy plus a world cue. This run added a shared `plannedExtractPosture` read in `src/game/simulation.ts`, surfaced it on the DOM `Extract Decision` card and Phaser `Extract Pressure` panel, and let convoy/casualty incidents pre-bias toward the focused pull so the chosen exit now shows concrete staged counts before the beacon burns. The latest evidence is `automation-artifacts/extract-posture-pressure-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: A focused hot extract with bankable haul should hard-commit one non-escort screen team before `Screen Exfil` is explicitly fired.
  - `Reason`: The previous pre-beacon extract pass still left some far pulls reading like pure recovery/fallback posture even when the player was loaded, the route was noisy, and a named boy was close to breaking. This run added a shared planned-extract commit profile in `src/game/simulation.ts`, retasks the first non-escort fireteam immediately when the focused pull is both hot and worth banking, and updates the shared `Pre-Beacon Posture` plus Phaser extract line to call out `hot commit` instead of hiding the decision inside support movement alone. The latest evidence is `automation-artifacts/extract-hard-commit-card-2026-04-19.png` and `automation-artifacts/extract-hard-commit-full-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Focused pre-beacon extract posture should buy a small real gameplay advantage instead of staying a pure readout until `Screen Exfil` is fired.
  - `Reason`: The selected pull already staged screens, recovery traffic, and fallback lanes, but it still felt too cosmetic because the actual hold timer and ring pressure stayed untouched until a later explicit command or a separate frontline incident paid out. This run added a shared `getPlannedExtractBenefit` layer in `src/game/simulation.ts`, so staged pull posture now softens nearby ring threats, delays incoming sweep pressure a little, shortens the eventual hold start, and surfaces that payoff in the DOM `Extract Decision` board plus the Phaser `Extract Pressure` panel. The latest evidence is `automation-artifacts/planned-extract-benefit-full-2026-04-19.png` and `automation-artifacts/planned-extract-benefit-card-2026-04-19.png`, with staged snapshots showing reads like `-0.9s hold | 2 softened`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should pre-beacon extract benefit stay capped as a light head start, or scale harder with large staged screens and recovery traffic so the chosen pull can become a genuinely safer bank call before `Screen Exfil`?
  - `Reason`: The new pass makes extract planning materially useful without auto-solving the final pull, but it currently tops out as a modest reduction in hold time plus lighter ring/sweep pressure. The next human choice is whether that restraint is the right tension for the first playable, or whether heavily staged exits should snowball into a much stronger pre-beacon bank window once multiple screens and recovery tracks are already on the chosen pull.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn the raid-side extract recommendation into a real live exfil choice surface before widening extraction work into deeper AI or economy logic.
  - `Reason`: The route data already carried multiple extract options with distinct summaries and pressure spawn plans, but the player still consumed that mostly as briefing flavor because the raid HUD only advised `leave` or `stay`. This run added a clickable exfil-choice stack to the `Extract Decision` board in `src/main.ts` and `src/styles.css`, wired it through a new `setFocusedExtract` controller path in `src/game/simulation.ts`, and updated `scripts/project-cli.mjs` plus `wiki/project-cli.md` so automation can stage a raid, focus a named extract, and capture either the full scene or just the decision card. The latest evidence is `automation-artifacts/extract-choice-card-2026-04-19.png` and `automation-artifacts/extract-choice-focus-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Picking an extract should also retask the live boys wedge and auto-bias world-space support cues toward that same pull instead of staying a player-only planning layer.
  - `Reason`: The prior extraction-choice pass made multi-exfil routes legible, but the squad still read too passive because exfil focus only changed board copy and light support drift. This run updated `src/game/simulation.ts` so the player-bound escort also leans toward the named pull before the beacon burns, added shared staged-cue markers for pre-beacon screen/recovery/fallback posture, and surfaced the same lean in `src/main.ts`, `src/styles.css`, and `src/game/scene/RaidScene.ts` through stronger `Planned Lean` / `Planned Exfil` reads. The latest evidence is `automation-artifacts/extract-lean-full-2026-04-19.png` and `automation-artifacts/extract-lean-card-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new pre-beacon `Planned Lean` / staged-cue layer stay visible any time an extract is focused, or collapse once the route quiets down unless the player hovers the extract board again?
  - `Reason`: The new pass finally makes exfil planning feel squad-owned because the boys wedge, focused extract card, and raid scene all react before `Screen Exfil` is committed. The next human choice is whether that stronger reminder should stay persistent to reinforce the extraction fantasy, or whether the HUD/scene should quiet down during calmer stretches once the player has already internalized the chosen pull.
  - `Decision owner`: Human directive needed.

- `Resolved`: Focused exfils should also retask named-boy lane callouts, in-scene `Boys Net` readouts, and frontline support foothold copy so the chosen pull is visible across the whole squad surface instead of only in extract-specific widgets.
  - `Reason`: The earlier extract-focus passes already leaned recovery/fallback behavior and stage cues toward the selected exit, but the named boys and support drawer still mostly read like ambient coverage unless the player manually inferred the connection. This run updated `src/game/simulation.ts` so planned exfil leaning now stamps explicit foothold copy onto staged fireteams, recovery tracks, and the escort wedge, and `src/main.ts` plus `src/game/scene/RaidScene.ts` now use shared planned-pull lane labels inside the live roster, `Boys Net`, and command summary surfaces. The latest evidence is `automation-artifacts/extract-retask-full-2026-04-19.png` and `automation-artifacts/extract-retask-drawer-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Once a focused extract is already in `hot commit`, the next escalation step should peel a second non-escort screen team automatically when a named boy goes critical or the ring flips contested.
  - `Reason`: The first hard-commit pass already made far pulls feel more decisive, but the raid still under-reacted when the squad was visibly close to breaking. This run updated the shared planned-extract commit profile in `src/game/simulation.ts` so a focused hot pull now escalates from one to two hard-committed screen teams whenever the ring is breaking or a named boy is critical, and the same shared posture/cue copy now names that second peel across the extract readouts instead of hiding it in support movement. The latest evidence is `automation-artifacts/extract-double-commit-full-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: After the new two-team hot commit triggers, should the system also auto-fire the actual `Screen Exfil` order if a second named boy goes critical, or keep that last escalation in the player's hands?
  - `Reason`: The pre-beacon extract layer now reacts much more convincingly to squad break risk by peeling a second screen team before the beacon burns, but the final question is whether the system should ever cross the line from squad-owned staging into actually spending the explicit order for the player. Keeping `Screen Exfil` manual preserves agency and tactical timing, while auto-firing it in the worst cases would make the game more protective of named boys once the pull is already collapsing.
  - `Decision owner`: Human directive needed.

- `Resolved`: Promote squad chatter into a shared raid-side `War Read` layer before widening scope into additional squad systems.
  - `Reason`: The direction document keeps pushing `me and the boys` as the emotional core, but the live raid still made squad chatter feel generic unless the player already knew how to read the command widgets. This run added shared squad-radio `channel` metadata plus a new exported `getSquadBattleRead` helper in `src/game/simulation.ts`, surfaced that as a dedicated `War Read` block in `src/main.ts` and `src/styles.css`, and mirrored the same read plus tagged net traffic into `src/game/scene/RaidScene.ts` so `Quiet Walk`, `Yellow Tape`, `Breach Stack`, and `Hot Extract` now read directly in screenshots and live play. The latest evidence is `automation-artifacts/squad-war-read-boys-command-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `War Read` stay persistent in the main `Boys Command` card, or only light up once contact, hot tape, or extraction pressure is actually live?
  - `Reason`: The new layer makes the raid's squad fantasy much easier to parse and gives the boys a stronger voice in the main frame, but it also spends permanent HUD budget on a second squad-status surface. The next human choice is whether to keep that always-on squad fantasy reinforcement, or collapse it during cold movement so the command card only expands once the lane stops being quiet.
  - `Decision owner`: Human directive needed.

- `Resolved`: Turn `make coffee in the trench for stamina` into a stash-queued frontline operation that stages a real raid-side brew pocket with a visible `Burner rush` payoff.
  - `Reason`: The direction document calls out trench coffee as a core frontline-life verb, but until this run it only existed as stash flavor text on rack items. This run added a new `Stage Burner Coffee` frontline order in `src/game/simulation.ts`, converted one route support pocket into a trench brew hold with ammo and a longer momentum buff, staged it for review through `src/main.ts`, and gave the raid scene warmer hold rings plus steam marks in `src/game/scene/RaidScene.ts`. The latest evidence is `automation-artifacts/burner-coffee-showcase-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should trench coffee stay a player-planned pre-raid order, or start appearing as a rarer ambient frontline opportunity you can discover mid-raid?
  - `Reason`: The new brew pocket works well as a deliberate stash-side prep choice because it teaches the verb cleanly and keeps the first playable readable. The next human decision is whether coffee should remain a controllable squad-prep lever, or whether the living-war fantasy is better served by sometimes stumbling into an already-burning trench pocket during a route.
  - `Decision owner`: Human directive needed.

- `Resolved`: Let ordinary raids occasionally spawn a lighter ambient thermos pocket, while keeping the stash-planned `burner-coffee` operation as the stronger version with extra sustain and a longer shove window.
  - `Reason`: The direction document treats trench coffee as frontline life, not only as a menu-side buff purchase. This run updated `src/game/simulation.ts` so scarred, moderately hot sectors can convert one shared recovery pocket into a lighter `Warm hands` thermos stop during live raids, while the existing stash operation still upgrades that same lane into the heavier `Burner rush` pocket with extra sustain. `src/main.ts` now stages a deterministic `field-coffee` showcase, and the latest evidence is `automation-artifacts/field-coffee-showcase-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should ambient thermos pockets stay a relatively quiet discoverable reward in moderately hot sectors, or should the next pass let squads explicitly call them out and route toward them once comms/readability budget opens up?
  - `Reason`: The new ambient pocket makes coffee feel like part of the living frontline instead of only a stash-side prep toggle, but it is still presented as a discovered opportunity rather than an explicit player-command target. The next human choice is whether that understated discovery tone is right for now, or whether the boys should begin announcing and steering around thermos pockets more aggressively once more comms/UI budget is available.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface worked frontline scars in the primary raid HUD and Phaser scene instead of leaving planted flags and recovered bodies mostly inside the tactical drawer.
  - `Reason`: The simulation already tracked reclaimed pockets, flag teams, and body-recovery scars, but that living-war memory still read like side-detail unless the player opened the tactical drawer or stared at world glyphs. This run added a shared `Scar Memory` card in `src/main.ts` and `src/styles.css`, plus a matching `SCAR MEMORY` panel in `src/game/scene/RaidScene.ts`, so the nearest worked scar now reads in the main raid frame with focus state, summary copy, and counts for flags, recoveries, and breaking cuts. The latest evidence is `automation-artifacts/frontline-scar-memory-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new `Scar Memory` surface stay pinned in the main raid HUD whenever any worked scar exists, or collapse after a few seconds unless a nearby scar is actively breaking?
  - `Reason`: This run made frontline memory much easier to read in play and screenshots, especially on the `territory-claims` showcase, but it also adds another always-on card to the bottom HUD band. The next human choice is whether that war-memory card should stay persistent so the route always feels historically loaded, or quiet down once the player has internalized the scar unless a live breaking pocket needs attention.
  - `Decision owner`: Human directive needed.

- `Resolved`: Spend the next boys-command readability pass on explicit world-space order beacons before widening scope into new command verbs.
  - `Reason`: The prior payoff and lane-tag passes made the current four orders legible in panels, but the actual raid frame still required too much UI reading to understand what the boys were pushing right now. This run added a shared `getFrontlineSupportOrderWorldCue` helper in `src/game/simulation.ts`, surfaced the same mark in the DOM `Boys Wedge` card in `src/main.ts`, and projected it into `src/game/scene/RaidScene.ts` as a live objective beacon with escort-to-mark rails, so `Stack Breach`, `Cover Me`, `Pull Eyes Off`, and `Screen Exfil` now read directly in the world. The latest evidence is `automation-artifacts/boys-command-world-cue-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new world-space boys-order beacon stay strong and always-on during active commands, or fade down once the player closes on the mark so the room fight regains visual quiet?
  - `Reason`: This run proved that a direct raid-side mark makes the squad's intent much easier to parse in motion and screenshots, especially during `Stack Breach`. The next human choice is whether that beacon should remain emphatic for the whole active order window, or taper once the player is effectively on top of the target so the top-down firefight stays cleaner.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface nearby hostile tape identity in the live raid HUD and scene before widening scope into deeper enemy-faction mechanics.
  - `Reason`: The vision document already names Blue, Green, and Yellow tape as a core battlefield language, but the actual raid still read too much like generic hostile dots. This run added a lightweight `tapeId` presentation layer in `src/game/simulation.ts`, surfaced a shared `Blue Tape Read` panel in `src/main.ts`, and mirrored the same hostile tape summary plus tape-color status strips into `src/game/scene/RaidScene.ts` so screenshots and playtests now communicate what kind of contact is stacked nearby. The latest evidence is `automation-artifacts/hostile-tape-readout-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Blue/Green/Yellow tape should stay readable, but the next combat pass should also give elite and foreign contacts sharper live behavior instead of leaving tape as flavor only.
  - `Reason`: The tape readouts were working, but the actual lane still fought too generically because tape mostly changed UI language. This run added shared tape-combat tuning in `src/game/simulation.ts` so Blue line troops shake longer under pressure, Green elites reacquire faster with cleaner spacing, and Yellow volunteers crowd angles harder with faster recovery. The DOM `Blue Tape Read` card in `src/main.ts` and the Phaser `Boys Net` line in `src/game/scene/RaidScene.ts` now also describe those behavioral differences directly. The latest evidence is `automation-artifacts/tape-identity-boys-command-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Surface squad recovery pressure as a dedicated `Recovery Cadence` board in debrief and the operator stash tab instead of leaving fatigue, reserve depth, and memorial fallout spread across separate roster widgets.
  - `Reason`: The simulation already tracked named losses, reserve cycles, fatigue, and readiness, but the stash still made the boys' recovery state feel like background bookkeeping. This run added a shared cadence board in `src/main.ts` and `src/styles.css` that compresses active-net count, average readiness, fatigue load, and best bench depth into one visible consequence surface, with copy that explicitly tells the player whether the next push should rotate, replace, or greed harder. The latest focused evidence is `automation-artifacts/squad-cadence-card-focus-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should squad recovery cadence stay advisory for now, or should the next pass start adding harder readiness penalties when the player ignores rotation and redeploys a burned stack?
  - `Reason`: The new board makes squad wear and replacement posture readable, but it still behaves as a planning surface rather than a forceful system. The next human choice is whether to keep that readable-but-soft Tarkov stash tone for a while longer, or to make overused operators suffer sharper deployment consequences so rotation becomes a stronger part of the extraction loop.
  - `Decision owner`: Human directive needed.

- `Resolved`: Surface boys command follow-through as explicit lane callouts across the existing raid HUD and escort labels instead of opening another command widget.
  - `Reason`: The previous posture pass made the wedge move differently per command, but the raid still read too much like abstract task tags unless a human already knew the system. This run added a shared escort-lane helper in `src/game/simulation.ts`, pushed that into the `Boys Command` summary and live squad cards in `src/main.ts`, and mirrored it into the Phaser `Boys Net` strip plus escort shoulder labels in `src/game/scene/RaidScene.ts` so commands now read as concrete slots like `CUT LEFT`, `STACK MID`, and `DRESS RIGHT`. The latest evidence is `automation-artifacts/boys-command-lanes-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the new boys lane callouts stay visible during ambient wedge movement, or collapse back until an active boys command is live?
  - `Reason`: This run proved that explicit lane callouts make the squad stack much easier to read in motion and screenshots. The remaining product choice is whether that language should remain on all the time so the wedge always reads tactically, or whether the calmer no-order state should reduce visual weight until the player actually issues `Cover Me`, `Pull Eyes Off`, `Screen Exfil`, or `Stack Breach`.
  - `Decision owner`: Human directive needed.

- `Resolved`: Deepen the current four `Boys Command` verbs with visible raid-side posture changes before widening scope into more command categories.
  - `Reason`: The command layer already had readable labels, hotkeys, and comms, but in motion the boys still held too much of one generic escort wedge. This run used the existing four verbs as the scope boundary and made each one feel more concrete in the playfield: the escort now re-forms differently per order in `src/game/simulation.ts`, and `src/game/scene/RaidScene.ts` now draws intent rails off each escort operator so the active posture reads immediately in raid. The latest evidence is `automation-artifacts/boys-command-order-posture-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.
- `Resolved`: Sharpen the four current boys-command verbs with a shared live payoff readout before widening scope into new command behaviors.
  - `Reason`: The command layer already had direct player verbs and readable task tags, but in the live raid the player still had to infer whether a command was buying anything beyond the initial bark. This run added a shared `getFrontlineSupportOrderPayoff` summary in `src/game/simulation.ts`, surfaced it in the DOM `Boys Wedge` strip in `src/main.ts`, mirrored it into the Phaser `Boys Net` panel in `src/game/scene/RaidScene.ts`, and exposed the same state in the CLI snapshot as `frontline.supportOrderPayoff`. The latest evidence is `automation-artifacts/boys-order-payoff-surface-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next boys-command pass keep the new payoff readout terse and tactical, or spend UI budget on more explicit lane markers and world-space command cues?
  - `Reason`: The current default makes each order's value legible through compact counts, timers, and short payoff text in both the HUD and the scene panel. The next human choice is whether to keep that compact top-down readability, or push the next command pass toward stronger in-world arrows, lane wedges, or order-specific markers once the current readout has had time to settle.
  - `Decision owner`: Human directive needed.

- `Resolved`: Standardize scav and frontline firefight gun output around the shared player weapon definitions instead of keeping separate AI-owned fire cadence, range, and damage tuning.
  - `Reason`: The directive explicitly called for AI-versus-AI firefights to use the same guns and projectile behavior as the player. This run rewired `src/game/simulation.ts` so enemy and frontline fire cadence, effective range, projectile speed, pressure type, and damage scaling are derived from `src/game/weapons.ts`, while archetypes now mainly own movement and behavioral posture. The new raid-side evidence is `automation-artifacts/shared-guns-boys-net-raid-2026-04-19.png`, and the `Boys Net` HUD in `src/game/scene/RaidScene.ts` now surfaces a live nearby gun mix line for human review.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Deliberately push more shotgun-bearing AI into authored room-clears and frontline breaches while keeping the shared combatant foundation intact.
  - `Reason`: The shared weapon parity pass was technically correct, but the room-clear slice still looked too rifle-heavy to sell close-quarters extraction pressure. This run added a shared `getFrontlineRoomCombatantLoadout` helper in `src/game/simulation.ts`, used it for live room-hold breach spawns plus the authored room-clear staging in `src/main.ts`, and surfaced the resulting CQB recipe in the `Room Stack` HUD so the same slice now reads as `2x VKR Rifle | 2x Morrow Shotgun` with `Blue 2 | Green 2 | Yellow 1` instead of another generic rifle stack. The latest evidence is `automation-artifacts/room-clear-cqb-loadouts-2026-04-19.png`, and `room-clear-chain` stayed green at `automation-artifacts/room-clear-chain-cqb-verify-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next close-quarters combat pass keep shotgun-heavy Green/Yellow tape pressure scoped to authored room stacks, or start pushing the same CQB recipe into ordinary live-building clears across the district?
  - `Reason`: This run proved that shared shotgun loadouts materially improve breach readability without splitting off a separate enemy combat system, but the clearest payoff still lives inside the authored room-clear slice. The next human choice is whether to keep polishing that deterministic first-playable showcase, or broaden the same CQB seeding rules into more organic building fights now that the room-stack recipe is working.
  - `Decision owner`: Human directive needed.

- `Resolved`: Keep live squad radio traffic visible in the main raid HUD and Phaser scene instead of hiding most of the boys' chatter in the tactical drawer.
  - `Reason`: The squad command layer already had useful comms and a rolling log, but the live raid still under-sold `me and the boys` because the freshest callouts were easy to miss unless the drawer was open. This run wired the existing squad log into a compact `Last Traffic` block on the main `Boys Command` card in `src/main.ts`, restored the missing `NET TRAFFIC` sync in `src/game/scene/RaidScene.ts`, and fixed the small TypeScript blockers in `src/game/simulation.ts` so the build is green again. The latest evidence capture is `automation-artifacts/boys-net-traffic-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the live traffic stay as terse radio barks, or should the next pass add more personality-heavy lines and side-specific flavor once the command readability is locked?
  - `Reason`: The traffic feed now reads clearly in the live raid, but the next human-facing scope choice is whether to preserve the current concise tactical phrasing for top-down readability, or spend the next squad pass on richer RimWorld-style banter that carries more character voice and war flavor.
  - `Decision owner`: Human directive needed.

- `Resolved`: Keep the boys command state in the primary raid HUD and drive live squad assignments/comms from the active order instead of leaving the command layer mostly inside the tactical drawer.
  - `Reason`: The direct-verb command pass already existed, but in the live raid it still took too much drawer reading to understand what the boys were actually doing. This run added a persistent `Boys Command` card in `src/main.ts`, pushed the current order/reset state into each live squad card, and updated `src/game/simulation.ts` so the boys' assignments and ambient callouts stay aligned to the active order while it is live. The latest evidence is `automation-artifacts/boys-command-command-surface-2026-04-19.png`, and `room-clear-chain` stayed green at `automation-artifacts/room-clear-chain-command-pass-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next command pass keep sharpening readability and combat payoff around the four current raid verbs, or spend scope on order follow-through like auto-clearing the next room and post-order fallback behavior?
  - `Reason`: The boys command layer now reads clearly in the main HUD and the squad's live assignments actually reflect the selected order. The next human-facing choice is whether to deepen those same four verbs with stronger behavioral payoff after the initial radio bark, or branch into a larger command feature set before the first playable command loop is fully proven.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new named order tags stay terse and tactical (`CUT`, `STACK`, `DRESS`, `SCREEN`) for top-down readability, or should the next squad pass expand them into richer role phrases or portraits once command depth grows?
  - `Reason`: This run tightened the boys command pass again by surfacing explicit task tags in the raid HUD cards, the Phaser `Boys Net` panel, and the escort shoulder labels. The current default makes the squad split readable at a glance during room clears, but the next human call is whether to preserve that compact military shorthand or spend UI budget on denser squad identity once the command verbs themselves deepen.
  - `Decision owner`: Human directive needed.

- `Resolved`: Keep the player-bound escort emphasized across the existing raid HUD and tactical map instead of adding another detached squad subsystem.
  - `Reason`: The simulation already had the escort wedge and the project already had a `Boys Command` surface, but the raid still under-sold `me and the boys` because the escort mostly looked like another generic support dot. This run used the existing HUD and tactical map surfaces to add a dedicated `Boys Wedge` strip, escort-highlighted squad cards, a map escort link, and a stronger in-scene shoulder-stack connector instead of widening scope into a brand-new command mode. The latest evidence is `automation-artifacts/boys-escort-readability-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Render the boys as individual in-raid operators during the escort wedge instead of relying on labels and a single aggregate support token.
  - `Reason`: The command surfaces were already live, but the room-clear push still did not look enough like `me and the boys` because the escort mostly read as one friendly support marker. This run layered individual escort bodies, role-weighted silhouettes, condition tinting, and live name tags into `src/game/scene/RaidScene.ts`, and also removed the tactical-map `playerPoint` staging crash in `src/main.ts` so the authored room-clear capture path works again. The latest evidence is `automation-artifacts/boys-wedge-room-clear-2026-04-19.png`, and `room-clear-chain` verification is green again at `automation-artifacts/room-clear-chain-boys-verify-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Keep live squadmate shoulder tags scoped to the player's escort wedge instead of stamping every friendly foothold in the district.
  - `Reason`: This run turned the escort fireteam into named shoulder-stack tags in `src/game/scene/RaidScene.ts` so the raid finally reads like `me and the boys` moving through the lane. Limiting those tags to the player escort keeps Rook, Makar, and Yara readable during breaches and extracts without flooding the wider frontline with floating nameplates from every ambient friendly support team.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: The stash debrief should surface a dedicated `Campaign Fallout` block that ties the last raid to current route control, urgent sector states, and recent raid history.
  - `Reason`: The previous debrief made the single run legible, but it still read like a detached after-action report. Pulling live sector control, support need, and recent-push memory into the same stash surface makes extraction feel like changing a living district instead of just closing a ledger entry.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Spawn the boys as a literal player-bound escort fireteam in raids instead of leaving squad presence mostly in comms, stash surfaces, and distant ambient footholds.
  - `Reason`: The directives explicitly call for the player to deploy with his boys and have them follow and fight for him. This run added a dedicated escort support unit in `src/game/simulation.ts` that snaps back onto the player after authored showcase teleports, follows as a close wedge, and inherits the selected player weapon via the existing shared support firearm logic. The latest evidence capture is `automation-artifacts/player-squad-escort-room-chain.png`, and the live snapshot now shows `support-901` holding within roughly `50.9` units of the player during the room-clear-chain showcase instead of reading as a distant ambient friendly.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the debrief `Campaign Fallout` block stay focused on the active route plus the two hottest neighboring sectors, or expand into a denser full-front overview once the living-war layer gets busier?
  - `Reason`: This run added a new debrief section that ties the last extraction to route control, support need, scars, and recent push history. The current default keeps it compact and readable for the first playable, but a fuller war-state overview may become worth the space once more sectors, convoys, and aftermath beats are on screen.
  - `Decision owner`: Human directive needed.

- `Resolved`: Spend the next squad pass on explicit raid-side command verbs for the boys by reframing the support-order layer into direct player-issued squad commands.
  - `Reason`: The escort wedge already made the boys physically present, but the live raid surface still read like a systems console instead of a player commanding his fireteam. This run retitled the four support orders into direct verbs (`Cover Me`, `Pull Eyes Off`, `Screen Exfil`, `Stack Breach`), surfaced them in both the tactical drawer and a new Phaser `Boys Net` command panel, and added a repeatable `boys-command` showcase with fresh evidence at `automation-artifacts/boys-command-showcase-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next squad-command pass stay focused on the current four high-level raid verbs, or deepen into more explicit per-position orders like hold house, watch treeline, and suppress window?
  - `Reason`: The raid now has a readable command layer that matches the `me and the boys` fantasy, but the human-facing scope choice is whether to keep sharpening readability and combat payoff around the four existing verbs first, or branch into more granular battlefield orders that risk widening scope before the first playable command loop is fully proven.
  - `Decision owner`: Human directive needed.

- `Open`: Should the next command-readability pass add deterministic showcases for `Cover Me`, `Pull Eyes Off`, and `Screen Exfil`, or keep the single breach-heavy `boys-command` proof slice until the underlying order payoffs deepen further?
  - `Reason`: This run made each command posture more distinct in the live raid view, but the authored screenshot path still centers mostly on `Stack Breach`. The next human-facing choice is whether to spend scope on proving the other three verbs with equally reviewable staged captures, or keep investing the time into the underlying order behavior first.
  - `Decision owner`: Human directive needed.

- `Resolved`: Delete the remaining room-hold-only doorway lane planner and doorway-specific committed-target latching so authored interiors traverse with the same simple opening logic as ordinary buildings.
  - `Reason`: The active directive still calls doorway-specific traversal over-engineered, and the remaining special casing lived in `src/game/simulation.ts` rather than in collider resolution. This run removed the bespoke doorway lane router, dropped doorway-only committed-target extension, and kept only simple doorway opening candidates plus generic obstacle bypassing. A fresh live playtest on `room-clear` and `room-clear-chain` still staged correctly with `playerRoomDepth=2`, `roomStackDepth=3`, and `stalledFireteams=0`, with the new evidence capture at `automation-artifacts/room-clear-simplified-routing.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next building simplification pass also remove the small room-hold doorway-forgiveness bonus from collision response, or keep that authored leniency while generic navigation is now shared?
  - `Reason`: The navigation side is now simplified across ordinary buildings and authored room stacks, but `getObstacleCollisionResponse` still gives `Open room hold` obstacles a slightly wider doorway cutout and shell slack than the rest of the map. The next product choice is whether to finish the directive literally by deleting that last room-hold-specific movement leniency, or preserve it as a light authored buffer for the staged room-clear showcase.
  - `Decision owner`: Human directive needed.

- `Resolved`: Keep the baseline authored `room-clear` slice readable and survivable, and let `room-clear-chain` carry the harsher chained follow-through pressure.
  - `Reason`: The previous room-clear staging dropped the player almost on top of immediate live defenders, which caused both authored room-clear verifier paths to collapse back to stash before they could prove the interiors were actually walkable. This run pushed the closest defenders deeper into the stack, let the boys establish interior cover faster, and shortened the chain verifier to validate the chained doorway walk directly. The result keeps both room-clear drills green while preserving layered back-room resistance (`roomDefenderCount=5`, `deepRoomDefenderCount=3`, `contestedRoomCount=2`).
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the next authored room-clear pass keep the thicker reserve stack that returned after doorway routing was scoped to room-hold interiors, or should automation spend the next pass on ordinary live-raid building traversal checks outside the authored room stack?
  - `Reason`: This run cut generic doorway waypoint routing and doorway-committed target latching out of ordinary buildings in `src/game/simulation.ts`, leaving that logic active only for authored `Open room hold` interiors. The result is a cleaner split between normal building traversal and the curated room-stack push: `doorway-regression` stayed green (`nearestDoorwayDistance=26.3`, `stalledFireteams=0`) and `room-clear-drill` is green again with a thicker layered interior stack (`roomDefenderCount=7`, `deepRoomDefenderCount=2`, `reserveRoomDefenderCount=4`, `contestedRoomCount=3`). The remaining product choice is whether the next run should capitalize on that regained reserve pressure, or spend time validating non-authored buildings now that generic routing is simpler.
  - `Decision owner`: Human directive needed.

- `Resolved`: Scope doorway-specific path routing and doorway commitment to authored room-hold interiors instead of all multi-door buildings.
  - `Reason`: The directives explicitly call doorway-specific traversal over-engineered and ask for buildings to behave like open space. This run updated `src/game/simulation.ts` so ordinary buildings now use collider-defined openings plus normal corner routing, while only authored `Open room hold` interiors retain doorway-target latching for the staged chained clear. `doorway-regression` remained green and `room-clear-drill` also stayed green with `playerRoomDepth=2`.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: The authored room-clear HUD and world guidance should use concise tactical callouts (`cut`, `stack`, `locker`) instead of surfacing long authored route names during live chained pushes.
  - `Reason`: The chained-room showcase was mechanically readable but still looked like a debug overlay because room and doorway markers were echoing authored route names. Switching the live guidance to short tactical verbs keeps the top-down fight legible while still telling the player what to do next.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Stabilize the project CLI around a fully booted control surface and capture verification snapshots before writing screenshot artifacts.
  - `Reason`: The authored `room-clear` slice and breach slice were healthy when staged directly, but the CLI verify path could still lose state by booting against an unsettled Vite page or by writing screenshot artifacts before reading the final agent snapshot. Waiting for a stable runtime, swapping page boot/reload to `domcontentloaded`, retrying recoverable agent-call reloads, and snapshotting before screenshot capture restored reliable `doorway-regression` and `room-clear-drill` verification without changing the underlying raid content.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should the in-raid `Extract Decision` board bias early toward banking stash value, or should it keep recommending one extra pocket until the route is much closer to a response spike or exfil crash?
  - `Reason`: This run added a live raid-side recommendation board that weighs bankable value, remaining upside, and route risk into a simple stay-vs-extract call. The open tuning question is how Tarkov-cautious the default advice should feel in the first playable: stash-protective the moment real value is on the operator, or greedier while the route is still technically manageable.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new in-scene `Extract Pressure` panel stay always visible during raids, or should it collapse until exfil is live or the player is carrying meaningful haul?
  - `Reason`: This run pushed the bank-vs-risk call directly into the Phaser playfield with a dedicated `Extract Pressure` panel plus an authored `extract-pressure` showcase. The remaining product choice is whether that coaching should always be on screen from raid start, or whether it should stay quiet until the route has real extract consequence on the board.
  - `Decision owner`: Human directive needed.

- `Open`: Should building cutaway reveal stay strictly player-driven, or should squadmates and nearby friendlies also peel roofs back when they commit through the same structure?
  - `Reason`: This run added a roof-fade cutaway pass in `src/game/scene/RaidScene.ts` so authored room-clear slices read like walkable interiors instead of sealed black boxes. The unresolved product choice is whether reveal should only track the player's current building for maximum visual discipline, or whether friendly occupancy should also expose nearby interiors so the squad war story is easier to read at a glance.
  - `Decision owner`: Human directive needed.

- `Resolved`: Remove the runtime doorway assist layer and let shared movement rely on segmented wall colliders plus normal axis sliding.
  - `Reason`: The directive explicitly calls out doorway-specific movement logic as over-engineered. This run deleted the live doorway steering helpers from `src/game/simulation.ts`, and the `doorway-regression` verification still passed with `nearestDoorwayDistance=26.3` and `stalledFireteams=0`, which means the breach lane still reads and moves correctly without those extra nudges.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Lead the stash debrief with an explicit consequence board that foregrounds outcome, net stash swing, haul, contract result, frontline fallout, recovered support stock, and boys status before the lower-level stat grid.
  - `Reason`: The simulation already computed meaningful extraction consequences, but the stash surface still read like a flat stat sheet. Promoting the economy and squad consequences into dedicated cards makes the extraction decision feel more like locking in or losing a war story, which better matches the project's core fantasy.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Surface authored room-stack progress directly in the live raid HUD instead of leaving chained-room readability mostly inside the tactical drawer and CLI telemetry.
  - `Reason`: The building-focused directives now have a functioning authored three-room clear, but the live raid view still hid most of that progress inside secondary surfaces. Adding a compact `Room Stack` card keeps the playfield readable while making chained-room depth, footholds, and defender pressure legible during the actual fight.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Collapse authored room-clear world markers to one concise marker per room on the player-adjacent chain instead of labeling every doorway exit.
  - `Reason`: The room-clear overlay already made chained interiors legible, but the world markers were still reading like debug text because every authored exit got a long route label. Collapsing the markers to the active chain and anchoring them to the doorway nearest the player preserves breach, follow-through, and deep-stash guidance while keeping the top-down firefight readable.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Treat the authored room-clear slice as a true follow-through foothold by staging the player in the second room and driving verification from the active room chain's interior settle anchors.
  - `Reason`: The doorway-collider simplification already kept the stack smooth, but the player showcase and drill were still reading like a first-doorway pause because telemetry exposed every authored room chain and the staged player hold sat on the first threshold. Narrowing the traversal targets to the player-adjacent chain and seating the player in the follow-through room makes the three-room clear read like gained interior ground without reintroducing bespoke door logic.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: The authored room-clear drills should settle the player in the follow-through room (`playerRoomDepth >= 2`) so the staged breach reads like real interior gain instead of a safe doorway anchor.
  - `Reason`: After the collider simplification, the remaining shallow finish was a staging and verification problem rather than a wall-collision problem. The automation now treats the deeper second-room finish as the default because it better matches the extraction-shooter fantasy of winning layered interior ground under pressure.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Simplify building collision by driving doorway openings from explicit wall collider segments plus doorway cutout volume instead of leaning only on the hollow-room shell test.
  - `Reason`: The active directive explicitly calls out over-engineered doorways and asks for straightforward colliders. Moving obstacle blocking to segmented wall colliders makes the doorway openings more direct to reason about while preserving the existing high-level navigation and breach systems.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Add a small doorway-lane hysteresis penalty so actors stay committed to the opening they are already threading instead of flipping to a nearby doorway mid-crossing.
  - `Reason`: The doorway assist and committed-target logic already reduced the worst wobble, but multi-exit rooms could still encourage lane swapping when the actor sat near overlapping doorway bands. Penalizing doorway candidates that would reverse the current crossing side keeps the shared movement resolver on the same lane a little longer and should make doorway traversal look more deliberate without broadening the whole movement system again.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Keep doorway commitment alive until the actor fully clears the doorway band instead of dropping the target on first approach.
  - `Reason`: The last doorway pass reduced shuffling, but the remaining entrance wobble comes from committed navigation still being released while the actor is straddling the threshold. Extending commitment through the doorway band keeps the shared movement resolver on one lane long enough for the actor to finish the crossing cleanly.
  - `Decision owner`: Automation run executed the implemented default.

- `Resolved`: Deepen the existing four boys-command verbs with contextual battlefield aliases before inventing a broader command taxonomy.
  - `Reason`: The command layer already had readable top-level verbs, but in motion the raid still made them feel like generic support buttons. This run kept the same four-command scope and translated live orders into more specific room-fight language in `src/game/simulation.ts`, `src/main.ts`, and `src/game/scene/RaidScene.ts`, so the squad now talks and renders in terms like `Suppress Window`, `Watch Tree Line`, `Hold Gate`, `Hold House`, and `Clear House` when the current pocket supports that read. The latest evidence is `automation-artifacts/boys-command-contextual-orders-2026-04-19.png`.
  - `Decision owner`: Automation run executed the implemented default.

- `Open`: Should these new contextual command aliases stay explicit in the main raid HUD, or should they eventually collapse back to the broader four command names once players learn the system?
  - `Reason`: This run proved that the more specific language makes the squad behavior much easier to read in screenshots and during a live room-clear, especially when the boys are stacked on a breach or screening an exit. The remaining human choice is whether to preserve that explicit authored language as part of the game's identity, or to treat it as training-wheel readability and revert the always-visible HUD copy to the simpler four-verb set later.
  - `Decision owner`: Human directive needed.

- `Open`: Should the new `route-echoes` dialogue pack stay tightly memory-gated and relatively rare, or should future runs let remembered-route chatter surface more often once players understand the baseline tactical language?
  - `Reason`: This run added a second reusable dialogue story pack under `src/game/dialogue/story-packs/route-echoes.ts` instead of widening resolver code, and the follow-up automation pass promoted its remembered-route payoff into a deterministic CLI proof. The pack keys remembered-route, hard-extract, civilian-carryover, and quiet-reset lines off the existing memory tags (`mate-recovered`, `mate-left-behind`, `civilian-saved`, `surrender-taken`, `sector-held`, `sector-lost`, and `extract-barely-made`), while `persistent-body-return` now forces Crosswind Docks into a live Yara body-return debt that keeps `Route Echo` comms and both raid-side plus roster-side `mate-left-behind` memories aligned. The remaining human choice is whether this memory layer should stay sparse and sharp like scar tissue, or become a more common audible identity once the pack library grows. The latest evidence is `automation-artifacts/route-echoes-persistent-body-return-2026-04-20.png`, `automation-artifacts/persistent-body-return-2026-04-20.png`, and `automation-artifacts/persistent-body-return-verify-2026-04-20.png`.
  - `Decision owner`: Human directive needed.

- `Open`: Should the authored `wounded-soldier` medical-hold slice stay as an always-available verified proof beat, or should future runs fold its casualty pressure back into broader live raid escalation instead of preserving a dedicated authored showcase?
  - `Reason`: The slice now reads much more clearly inside the raid itself instead of depending on the DOM-side operation summaries to explain why the lane matters. Relay Hamlet can still surface a focused `wounded-soldier` firefight where `Pressure Posture` says `Keep Yara on the line`, `Operation Flow` flips to a `survival` intent around Yara's hot shoulder, and squad doctrine changes to `Medical hold package`, but the Phaser view now also adds a dedicated `WOUNDED SOLDIER` / `MEDICAL HOLD LIVE` aftermath board, a med-lane boys-net title, and casualty / medic / satchel / hold-line dressing around the focused incident so the player can read the split at a glance. The remaining product choice is whether to keep this casualty-rescue pressure explicit as a reusable authored north-star proof, or to let future casualty systems absorb it so the game spends more authored surface on broader raid variety. Evidence: `automation-artifacts/wounded-soldier-medical-read-2026-04-20.png` and `automation-artifacts/wounded-soldier-verify-2026-04-20-medical-read.png`.
  - `Decision owner`: Human directive needed.
